# network-renderer-nixos regression state

- state=solved | target=hostile-dns-east-west-focused-check-too-slow | evidence=2026-06-02 `timeout 20 env NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-hostile-dns-east-west.sh` timed out after `elapsed_ms=20015` because the test compiled the `s-router-overlay-dns-lane-policy` example and evaluated a full `nixosSystem` for one route-materialization predicate. The test now exercises the owning SDS implementation surface directly by importing `s88/ControlModule/render/container-networks.nix` with a synthetic CPM-shaped hostile DNS/east-west policy model. Focused proof passed under the FS-940 hypothesis: `timeout 10 env NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-hostile-dns-east-west.sh` returned `rc=0 elapsed_ms=2801`. | expected=The hostile DNS east-west renderer regression shall prove route and policy-rule materialization through a focused renderer control-module check that completes under the warm-cache readiness budget. Full example compilation and NixOS option evaluation may remain separate SIT coverage, but they must not be the first or only proof surface for this module-specific predicate. | owner=network-renderer-nixos | reason=This was a renderer test-design and FS-940/FS-981 readiness bug, not a CPM semantic bug or a SAT acceptance fix. The control module owns materializing explicit CPM route facts into NixOS route and RPDB records.

- state=solved | target=overlay-return-sourcefile-and-runtime-origin-rule-scope | evidence=2026-06-02 full-loop network repo sweep failed three focused renderer tests after the delegated-prefix lane fix: `tests/test-return-side-policy-rules-unscoped.sh` found a policy upstream return interface was tenant-source scoped instead of keeping a broad self-ingress reply rule; `tests/test-runtime-origin-preferred-source-routes.sh` found same-interface runtime-origin static source-prefix rules were missing; `tests/test-overlay-core-local-hostile-return-routes.sh` found the hostile delegated GUA return route missing from table 2001. The patch keeps broad self-ingress rules only for actual return-side interfaces, applies same-interface static source-prefix scope for runtime-origin egress, and permits same-interface dynamic source-file scope on overlay/core tables while suppressing it on upstream-selector policy interfaces so delegated hostile prefixes do not select an unrelated WAN/core table. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-preferred-source-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-core-local-hostile-return-routes.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dynamic-sourcefile-policy-table-lane-selection.sh`; adjacent guards passed: `tests/test-runtime-sourcefile-routes-do-not-cross-policy-tables.sh`, `tests/test-dynamic-source-forwarding.sh`, and `tests/test-hostile-overlay-policy-routing-scope.sh`. | expected=Renderer policy-routing aggregation must distinguish three cases: return-side self-ingress keeps a broad reply rule, same-interface runtime-origin static source prefixes still get source-scoped policy rules, and delegated dynamic source-file rules are emitted only for the table that owns the modeled overlay/core path, not for upstream-selector policy interfaces carrying unrelated WAN defaults. | owner=network-renderer-nixos | test=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-preferred-source-routes.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-core-local-hostile-return-routes.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dynamic-sourcefile-policy-table-lane-selection.sh`

- state=solved | target=overlay-underlay-sourcefile-policy-table-cross-lane | evidence=2026-06-02 full-loop SAT failed site-C DNS and hostile/client public egress. Live proof on `nixos-router-core-nebula` showed Nebula handshakes to the Hetzner lighthouse timed out and `ping 10.90.10.1` failed. Hetzner tcpdump while probing UDP `95.217.189.33:4242` from NixOS saw no packet arrive. Hop tracing moved the first local failure to `nixos-router-upstream`: table `2001` for the ISP-B client lane selected a more specific dynamic route to `/run/secrets/hetzner-lighthouse-public-ipv4` via `core-a` (`10.10.0.12`) even though `core-isp-a` had no WAN lease/default in this run, while `core-b` was live and the table default already pointed to `10.10.0.14`. Artifact proof showed CPM supplied source-file endpoint routes on multiple core uplink interfaces; the renderer grouped dynamic delegated route candidates by `sourceFile|table` and projected each source-file route into every policy table, letting the lexicographically first interface win for unrelated lane tables. The renderer now projects table-scoped dynamic delegated routes only for explicit `runtime-routed-prefix-return` routes, preserving existing dynamic source-file return behavior while preventing `overlay-underlay-endpoint` routes from being fanned into unrelated policy tables. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-sourcefile-routes-do-not-cross-policy-tables.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dynamic-source-forwarding.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-underlay-endpoint-source-routes.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`. Full suite proof ran `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh`; all implementation tests passed and only this stale regression-state gate failed before the entry was promoted. | expected=Runtime endpoint source-file routes must not be installed into unrelated policy tables. A policy table may use its modeled default or explicit policy route for the selected lane, but the renderer must not fan one uplink's source-file host route into every table and override another lane's egress path. | owner=network-renderer-nixos | test=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-sourcefile-routes-do-not-cross-policy-tables.sh`

- state=solved | target=s-router-nixos-synthetic-wan-uplink-collapse | evidence=2026-06-02 full-loop SAT evidence failed endpoint internet from `s-router-test-clients:clab-client01`: IPv4 ping to `1.1.1.1` returned `From 10.10.0.14 Destination Net Unreachable`, and IPv6 ping to `2606:4700:4700::1111` returned `From fd42:dead:beef:1000::c Destination unreachable: No route`. Live route inspection resolved those sources to `nixos-router-core-isp-b` and `nixos-router-core-isp-a`; both core containers had explicit `DHCP=ipv4`, `IPv6AcceptRA=true`, NAT on `eth0`, but no default route. Rendered artifacts showed CPM supplied separate WAN contracts (`isp-a` on `uplink-isp-a`/`br-uplink0`, `isp-b` on `uplink-isp-b`/`br-uplink1`), while renderer attach targets collapsed `isp-a` to `assignedUplinkName = "uplink-isp-b"` and `renderedHostBridgeName = "br-uplink1"`. Live bridge state confirmed both core WAN veths were on `br-uplink1` and `br-uplink0` had only VLAN 4. The renderer now uses a unique upstream/uplink match for each WAN group before falling back to the host-wide default uplink when no explicit group map exists. Focused proof passed: `bash tests/test-host-uplink-vlan-dhcp.sh`, `bash tests/test-missing-wan-group-assignment.sh`, `bash tests/test-static-host-bridge-wan.sh`, `bash tests/test-core-ipv6-nat-rendering.sh`, and `bash tests/test-nixos-target-required-substrate-refusal.sh`. Full proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` returned `PASS network-renderer-nixos: 110 tests`. | expected=Synthetic WAN attach targets must preserve explicit CPM/inventory upstream identity across NixOS materialization: `isp-a` attaches to `uplink-isp-a`/`br-uplink0`, `isp-b` attaches to `uplink-isp-b`/`br-uplink1`, and an existing partial `wanGroupToUplink` map still fails strict validation rather than being repaired by fallback. | owner=network-renderer-nixos | reason=CPM and inventory supplied explicit WAN and host-uplink facts. The bad layer was NixOS renderer WAN attachment assignment, which applied the host default uplink to all WAN groups before honoring unique upstream/uplink matches.

- state=solved | target=provider-overlay-runtime-interface-name-limit | evidence=2026-06-02 the SAT `s-router-hetzner-anywhere` NixOS build advanced past the SOPS source failure and then failed nftables validation because provider overlay logical names such as `overlay-wg-routed64` and `overlay-wg-host128-egress` were emitted directly in `iifname`/`oifname` matches, exceeding Linux's 15-character interface-name limit. The renderer now maps provider-owned overlay logical names through the existing deterministic container interface naming module before firewall and container-network materialization, preserves logical/requested names in `interfaceAliases`, and keeps runtime-bound nftables/networkd artifacts on platform-valid names. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-container-firewall-ifname-limit.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-static-policy-rules.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nix-file-loc.sh`; full renderer suite is rerun before commit. | expected=Logical overlay/provider identities may be longer than the Linux runtime interface-name limit, but NixOS runtime-bound networkd, nftables, and route artifacts must use deterministic valid interface names with an audit mapping back to the logical CPM/provider identity. | owner=network-renderer-nixos | reason=CPM/provider output owns logical overlay identity; the NixOS renderer owns Linux runtime interface-name materialization from that explicit identity.

- state=solved | target=kea-static-client-reservation-projection | evidence=2026-06-02 the NixOS renderer consumes CPM-resolved reservation records and emits Kea DHCPv4 and DHCPv6 reservation entries without recalculating addresses from prefixes or offsets. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-kea-reservations-render.sh`; the full renderer suite also reached and passed that focused test before the stale regression-state entry was promoted. Trace retagged to `FS-970-HDS-010-SDS-010-SMS-010`. | expected=Kea DHCPv4/DHCPv6 reservation output contains the CPM reservation identity and CPM-resolved address. Missing CPM reservation contracts remain absent. Renderer code must not read intent or inventory to repair missing reservation data. | owner=network-renderer-nixos | reason=NixOS owns Kea materialization only. CPM owns reservation resolution and uniqueness.

- state=solved | target=explicit-slaac-radvd-flag-projection | evidence=2026-05-30 SMT/SIT preparation for `USR-MODEL-001-FS-001-HDS-002-SDS-001-001-SMS-001-015` found the radvd projector hardcoded `AdvManagedFlag off`, `AdvOtherConfigFlag off`, `AdvOnLink on`, and `AdvAutonomous on` in `s88/ControlModule/access/render/radvd.nix`. That made the renderer invent SLAAC/RA behavior instead of consuming explicit CPM `advertisements.ipv6Ra[]` fields. The renderer now requires explicit `managed`, `otherConfig`, `onLink`, and `autonomous` fields in each CPM IPv6 RA scope and projects those values into radvd. Focused SMT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-radvd-slaac-flags-render.sh`. Focused SIT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-slaac-pipeline-render.sh`. | expected=When CPM supplies explicit `advertisements.ipv6Ra[]` fields for managed, other-config, on-link, and autonomous/SLAAC behavior, the NixOS renderer must carry those values through the advertisement lookup and render the matching radvd flags. When the CPM contract is absent, no radvd/SLAAC output is emitted. | owner=network-renderer-nixos | reason=This is target materialization of explicit CPM address-assignment behavior. CPM owns whether the SLAAC flags exist; the NixOS renderer owns the radvd artifact projection.

- state=solved | target=explicit-ra-service-projection | evidence=2026-05-30 SMT/SIT preparation for `USR-MODEL-001-FS-001-HDS-002-SDS-001-001-SMS-001-014` found the NixOS renderer still had a derived RA path: `s88/ControlModule/access/lookup/advertisements.nix` selected `derived.radvdScopes` when no authoritative CPM advertisement existed, and `s88/ControlModule/access/lookup/advertisements/interfaces.nix` could mark RA renderable from role/interface defaults. The renderer now emits radvd scopes only from explicit `runtimeTarget.advertisements.ipv6Ra[]` and leaves absent RA contracts absent. Focused SMT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-radvd-explicit-only-render.sh`. Focused SIT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-radvd-pipeline-render.sh`. | expected=When CPM supplies `runtimeTarget.advertisements.ipv6Ra[]`, the NixOS renderer must emit deterministic radvd service fragments from those explicit fields. When CPM does not supply an IPv6 RA contract, the renderer must not derive RA service behavior from role defaults, interface addresses, interface names, node names, tenant names, or local inventory parsing. | owner=network-renderer-nixos | reason=This is target materialization of an explicit CPM address-assignment contract. CPM owns whether the RA contract exists; the renderer owns the NixOS/radvd fragment.

- state=solved | target=explicit-dhcpv6-service-projection | evidence=2026-05-30 SMT/SIT preparation for `USR-MODEL-001-FS-001-025-002` found `network-renderer-nixos` only projected DHCPv4 (`kea-dhcp4-*`) and RA (`radvd-*`) address-assignment services. Code inspection showed `s88/ControlModule/access/lookup/advertisements/authoritative.nix` only read CPM `advertisements.dhcp4` and `advertisements.ipv6Ra`, and `s88/ControlModule/access/render/default.nix` only rendered `kea.nix` and `radvd.nix`. The renderer now reads explicit CPM `advertisements.dhcpv6[]`, builds DHCPv6 scopes only when present, and emits Kea DHCPv6 services. Focused SMT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-kea-dhcpv6-render.sh`. Focused SIT proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-dhcpv6-pipeline-render.sh`. Full renderer suite `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` completed implementation tests with one expected stale-state failure before this entry was marked solved. | expected=When CPM supplies `runtimeTarget.advertisements.dhcpv6[]`, the NixOS renderer must emit deterministic DHCPv6 service fragments from those explicit fields and must not derive DHCPv6 service behavior from interface names, node roles, tenant names, or local inventory parsing. Absent DHCPv6 contracts must remain absent. | owner=network-renderer-nixos | reason=This is target materialization of an explicit CPM client-addressing contract. CPM owns whether the DHCPv6 contract exists; the renderer owns the NixOS/Kea DHCPv6 fragment.

- state=solved | target=explicit-interface-mtu-networkd-projection | evidence=2026-05-30 SMT/SIT preparation row `USR-MODEL-001-FS-001-020-005` failed because the NixOS renderer summary had `anyInterfaceMtu=false`. The NixOS renderer normalized CPM interfaces but `s88/ControlModule/render/container-networks/interface-units.nix` emitted systemd-networkd units without projecting an explicit interface `mtu` field to networkd link configuration. The renderer now emits `linkConfig.MTUBytes` from explicit `iface.mtu`. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-interface-mtu-render.sh`. Full renderer proof passed all implementation tests in `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh`; the first run failed only because this regression entry was still marked `fixed-locally` before being solved. | expected=When CPM/provider input contains `interfaces.<name>.mtu`, the NixOS renderer must emit a NixOS/systemd-networkd MTU artifact for that exact rendered interface. It must not infer MTU from role, interface name, WAN/provider type, or PPPoE assumptions. | owner=network-renderer-nixos | reason=This is target materialization of an explicit renderer-neutral CPM/provider fact. The renderer owns the NixOS `systemd.network.networks.*` projection, while CPM owns whether the MTU field exists.

- state=solved | target=2026-05-29-hostile-overlay-policy-table-scope-leaks | evidence=The original live proof showed hostile/east-west traffic selecting wrong core/policy tables even though compiler and CPM contracts were correct: `pol-hostile-ew` selected non-hostile defaults, Hetzner selected the wrong `core` table, and unrelated policy downstream ingresses could select hostile GUA egress. The renderer now keeps L4/trafficType-only accepts as firewall-only for route selection, preserves hostile IPv4/GUA source-scoped selectors on the hostile ingress, keeps source-scoped and destination-scoped selectors side by side for the same table, and prevents target-side hostile scope from leaking onto admin/client/mgmt/streaming ingress. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-hostile-overlay-policy-routing-scope.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-source-scoped-routing-rules.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-routing-source-interface-rules.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-lane-route-scoping.sh`. A full `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh` run completed all renderer CODE tests with only this stale unresolved-state gate failing before this entry was marked solved. | expected=L4/trafficType-only forwarding accepts may render firewall policy but must not project default routes into Linux policy tables. Source-side ingress such as `pol-hostile-ew -> core-nebula` must keep source-scoped RPDB rules for hostile IPv4/GUA even when the same table also needs destination-scoped return rules. Target-side/return source prefixes from `up-hostile-ew` or `core-nebula` must not be copied onto unrelated downstream ingress interfaces. | owner=network-renderer-nixos | reason=The compiler path and CPM route contracts are correct. The bad layer is NixOS renderer materialization of CPM forwarding/route/source-scope contracts into Linux route tables and routing policy rules. | test=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-hostile-overlay-policy-routing-scope.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh`.

- state=solved | target=2026-05-29-return-side-policy-table-broad-ingress-leak | evidence=Current FAT hostile overlay egress route-only scan fails after the live overlay/DNS route hotpatches made endpoint egress work. On `nixos-router-policy`, `ip rule` contains broad return-side table selectors such as `from all iif downstr-admin lookup 2006`, `from all iif downstr-client lookup 2006`, `from all iif downstr-stream lookup 2006`, `from all iif downstream-dmz lookup 2006`, and `from all iif downstr-hostile lookup 2006`. The validator report shows a hostile source can select the admin return table from unrelated ingress: `ip -4 route get 1.1.1.1 from 10.20.70.100 iif downstr-client` returns `via 10.10.0.31 dev up-admin-a table 2006`. On `nixos-router-upstream`, broad core return table selectors similarly let `pol-admin-a`, `pol-mgmt-a`, and `pol-stream-a` route hostile source `10.20.70.100` through `core-a table 2000`, with symmetric `*-b` selectors through `core-b table 2001`. The renderer now calculates rule source scope per ingress: real return-side ingress keeps the unscoped reply rule, but source-side tenant ingress uses the ingress interface tenant owner scope before pair scopes are added. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`, `tests/test-policy-source-scoped-routing-rules.sh`, `tests/test-policy-routing-source-interface-rules.sh`, `tests/test-lane-route-scoping.sh`, `tests/test-upstream-runtime-origin-source-lane-rules.sh`, and `tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`. | expected=Return-side policy tables may accept unscoped `iif` rules only from the return-side ingress interfaces that carry replies with public/foreign source addresses. Source-side tenant ingress into those same tables must remain source-scoped to the ingress tenant prefix or destination-scoped to an accepted return destination; unrelated ingress must not install `from all iif <source> lookup <return-table>` rules. | owner=network-renderer-nixos | reason=CPM already exposes lane ownership and tenantPrefixOwners. The bad live state is emitted Linux RPDB materialization: the renderer cleared source scope for the target return-side table before calculating pair-specific ingress rules, so that empty scope was applied to every source interface, not only the real return ingress. | test=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`; full renderer suite must pass before commit.

- state=solved | target=downstream-selector-explicit-access-owner-return-routes | evidence=2026-05-29 full loop failed `tests-from-s-router-test-clients` at `clab-streaming01 ping 1.1.1.1` and failed Cast TCP exposure from client endpoints to `clab-streaming01:8008/8009`. Live route tracing showed forward path and core NAT worked: replies reached `nixos-router-downstream` on `policy-stream`, but `ip route get 10.20.50.101 from 1.1.1.1 iif policy-stream` selected `via 10.10.0.29 dev policy-stream table 2005`, looping back to policy instead of returning to the streaming access leg. A reversible live patch on `nixos-router-downstream`, `ip route replace 10.20.50.0/24 via 10.10.0.10 dev access-stream table 2005 onlink` and `ip -6 route replace fd42:dead:beef:50::/64 via fd42:dead:beef:1000::a dev access-stream table 2005 metric 1024 onlink`, changed the route lookup to `dev access-stream`, made `clab-streaming01 ping -c2 1.1.1.1` pass, and made `clab-client01 -> clab-streaming01:8008` plus `clab-client02 -> clab-streaming01:8009` pass. Artifact proof split this away from CPM: `control-plane.json` has `tenantPrefixOwners` entries owned by `nixos-router-access-streaming` and the access/policy interfaces both carry `backingRef.lane.access = "nixos-router-access-streaming"`, but the renderer generated the return table without `10.20.50.0/24 via 10.10.0.10 dev access-stream table 2005`. The renderer helper still derived return destinations from rendered names, turning `access-stream` into tenant key `stream` instead of using explicit access owner metadata. Current focused proof added `tests/test-downstream-selector-explicit-access-owner-return-routes.sh`, which failed before the change and now passes; adjacent proofs also pass: `tests/test-return-side-policy-rules-unscoped.sh`, `tests/test-downstream-selector-return-connected-routes.sh`, `tests/test-policy-only-routes.sh`, and `tests/test-policy-routing-source-interface-rules.sh`. | expected=Downstream-selector return tables must derive local tenant return destinations from explicit CPM access ownership (`backingRef.lane.access` and `site.tenantPrefixOwners`) before falling back to rendered-name compatibility. Streaming, client, hostile, and any future access name must use the same generic owner contract; the renderer must not rely on abbreviations or interface-name tenant parsing when CPM supplies owner metadata. | owner=network-renderer-nixos | reason=The NixOS renderer owns Linux table materialization from explicit CPM route/owner data. The live route patch and generated networkd files proved the missing `Table=2005` route was a renderer projection problem, not an intent, NFM, CPM, or orchestration problem.

- state=solved | target=remote-overlay-egress-policy-table-wan-default-leak | evidence=2026-05-29 locked full-loop reached live overlay validation, but hostile public egress died after crossing Nebula into Hetzner. Live proof on `hetz-router-upstream`: `ip route get 1.1.1.1 from 10.20.70.100 iif nebula-core` selected `via 10.80.0.4 dev core table 2001`; IPv6 from the hostile delegated GUA selected `via fd42:dead:cafe:1000::4 dev core table 2001`. Current local renderer proof adds the remote overlay fixture to `tests/test-lane-route-scoping.sh` and passes: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-lane-route-scoping.sh` verifies that the `nebula-core` ingress table contains the explicit defaults via `pol-dmz-ew`/`fd42:dead:cafe:1000::e` and does not contain WAN/core defaults via `core`/`fd42:dead:cafe:1000::4`. Follow-up artifact proof split the remaining return failure to CPM: when CPM supplies the policy-WAN hostile return route, the renderer emits the policy-table route and scoped rule; the missing live route is fixed in `network-control-plane-model` by its delegated overlay public-egress SMT. | expected=For upstream selectors, Linux policy tables selected by an east-west/core-nebula ingress may project only CPM routes whose output lane is the same east-west lane or an explicit accepted east-west policy output. WAN/core defaults for other uplinks must not be imported into that table merely because the same upstream selector also has a normal WAN core. | owner=network-renderer-nixos | reason=The NixOS renderer owns translating explicit CPM route and forwarding facts into Linux RPDB tables. Current renderer output preserves the overlay-ingress table boundary; the missing return route was not solved by renderer inference and is owned by CPM contract augmentation.

- state=solved | target=hostile-overlay-dns-policy-table-return-routes | evidence=2026-05-29 locked full-loop reached live FAT and failed hostile/site-C DNS. Live proof showed two missing policy-table route classes in the NixOS materialization layer. On Hetzner `hetz-router-nebula-core`, DNS replies from `10.90.10.1` to hostile `10.20.70.101` selected an underlay/upstream path until reversible routes were installed with `ip route replace 10.20.70.0/24 dev overlay-west table 2000`, `ip route replace 10.20.70.0/24 dev overlay-west table 2001`, `ip -6 route replace fd42:dead:beef:70::/64 dev overlay-west table 2000`, `ip -6 route replace fd42:dead:beef:70::/64 dev overlay-west table 2001`, and matching hostile GUA `/64` table routes. The synced CPM artifact already exposes `hetz-router-nebula-core` provider overlay `overlay-east-west` as runtime interface `overlay-west` with explicit hostile overlay reachability routes, so the renderer was dropping explicit provider-neutral route facts from selected policy tables when overlay reachability routes had `dst` but no gateway/scope. A second live proof on `nixos-router-access-hostile` showed IPv6 DNS replies from `fd42:dead:cafe:10::1` to the hostile client looping on `transit`; `ip -6 route get fd42:dead:beef:70:<client> from fd42:dead:cafe:10::1 iif transit` selected table `2000` back to `transit` until reversible routes `ip -6 route replace fd42:dead:beef:70::/64 dev tenant-hostile table 2000` and `table 2001` were installed. After that, `sudo nixos-container run nixos-hostile-node01 -- dig -6 +time=2 +tries=1 @fd42:dead:cafe:10::1 iana.org AAAA` returned `NOERROR`. CPM already exposes `tenant-hostile.addr6 = fd42:dead:beef:70::1/64`; the renderer was deriving expanded IPv6 `/64` addresses as invalid `...:1::/64` prefixes. Focused tests now cover both failures: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-link-scope-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-static-policy-rules.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-return-connected-routes.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-core-local-hostile-return-routes.sh` pass after proving the tightened assertions failed before the patch. | expected=Provider-created overlay interfaces must keep provider-owned interface lifecycle, but every explicit CPM/provider-neutral on-link overlay reachability route needed for an accepted forwarding leg must be materialized as a provider route service in the selected policy tables. Every NixOS-owned interface prefix derived from explicit CPM addresses must also be present as a connected link route in policy tables where destination-scoped rules can select that table, so return traffic to connected tenants cannot loop back to transit/upstream. | owner=network-renderer-nixos | reason=The NixOS renderer owns Linux route and RPDB materialization from explicit CPM/provider output. The failing facts are present in CPM and fixed by reversible Linux route additions, so this is not an intent, inventory, NFM, CPM, or lab harness ownership issue.

- state=solved | target=normalized-explicit-forward-pairs-drop-traffic-type-and-steal-policy-routes | evidence=2026-05-28 locked full-loop plus live inspection showed `nixos-router-core-nebula` and normal endpoints were still not SAT-ready after renderer `eaa63c0`: `nixos-router-upstream ip rule` contained cross-lane unscoped selectors `2000: from all iif pol-client-b lookup 2000` and `2001: from all iif pol-client-a lookup 2001`; `ip route get 1.1.1.1 from 10.20.20.101 iif pol-client-a` selected `via 10.10.0.14 dev core-b table 2001` while nft only broadly allowed `pol-client-a -> core-a`, and the symmetric `pol-client-b` path selected `core-a` while nft only broadly allowed `pol-client-b -> core-b`. CPM preserved the L4 scope on the overlay-underlay rules as `trafficType = "nebula"` / `trafficType = "nebula-runtime"` for `pol-client-a/b -> core-a/b` with no `sourcePrefixes`; direct artifact query over `/etc/network-artifacts/control-plane.json` showed the broad L3 rules separately as `pol-client-a -> core-a` and `pol-client-b -> core-b`, plus source-scoped `runtime-origin-egress`. Renderer inspection found `s88/ControlModule/render/container-networks/policy-routing/forwarding-rules.nix` kept `trafficType` for `forwardingIntent.rules` but dropped it when expanding `normalizedExplicitForwardPairs`, so the existing `source-interfaces.nix` L4 guard never saw the traffic type on that copy. Reversible live deletion of the cross unscoped rules changed the route lookup away from the bad tables, proving the bad RPDB entries were active route selectors, though additional SAT blockers still remained. The renderer now preserves `trafficType` and explicit `match` from normalized explicit forward pairs before policy-route source-interface selection. Proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-routing-source-interface-rules.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-upstream-runtime-origin-source-lane-rules.sh`, and a full `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` run had all implementation tests pass with only this entry's temporary unresolved-state gate failing before it was marked solved. | expected=Normalized explicit forward pairs must preserve `trafficType` and any explicit L4 `match` data when converted to renderer forwarding rules. L4-only pairs may still render nft accepts, but they must not create unscoped Linux RPDB `iif -> table` selectors unless CPM also supplies source scope. | owner=network-renderer-nixos | reason=The NixOS renderer owns the projection from explicit CPM/normalized forwarding pairs into Linux RPDB rules. Dropping L4 metadata during a local normalization step violates the renderer README boundary by changing a firewall-only L4 allow into an L3 route selector.

- state=solved | target=traffic-type-forward-rules-steal-policy-routes | evidence=2026-05-28 live underlay proof showed `nixos-router-core-nebula` and normal client traffic could not ping `1.1.1.1` because `nixos-router-upstream` route selection sent `pol-client-b` ingress to `core-a` table `2000`: `ip route get 1.1.1.1 from 10.20.20.100 iif pol-client-b` returned `via 10.10.0.12 dev core-a table 2000`. The generated nft broad allow for normal client-b egress was `pol-client-b -> core-b`; `pol-client-b -> core-a` existed only for Nebula traffic types UDP/TCP 4242 and UDP 443. Reversible live rules `iifname "pol-client-b" oifname "core-a" accept` and `iifname "core-a" oifname "pol-client-b" accept` made `nixos-client01 ping 1.1.1.1` pass, made core-nebula `ping 1.1.1.1` pass, and allowed `s-router-nebula-render-node.service` to stay active with successful pings to Hetzner overlay peers `100.96.10.3` and `fd42:dead:beef:ee::3`. Artifact proof showed CPM had trafficType-only `pol-client-b -> core-a` rules for Nebula underlay plus broad `pol-client-b -> core-b` egress; the renderer incorrectly let the trafficType-only rule create an unscoped RPDB rule, even though Linux policy routing cannot match L4 traffic type. The renderer now ignores trafficType/L4-only forwarding rules when choosing unscoped policy-routing source interfaces unless the rule also has source scope. Focused proof passed: `bash tests/test-traffic-type-forward-rules-do-not-steal-policy-routes.sh`, `bash tests/test-policy-routing-source-interface-rules.sh`, and `bash tests/test-upstream-runtime-origin-source-lane-rules.sh`. | expected=TrafficType-only forwarding intent must still render firewall policy, but it must not create an unscoped `iif <source> lookup <target-table>` rule that captures all traffic from that source interface. Broad or source-scoped L3 forwarding rules may drive RPDB source selection; L4-only rules need explicit L3 source scope before they affect routing. | owner=network-renderer-nixos | reason=The NixOS renderer owns Linux RPDB materialization from CPM forwarding intent. CPM preserved the distinction between broad egress and Nebula traffic; the renderer collapsed that distinction into a route rule that Linux could not enforce at L4.

- state=solved | target=provider-overlay-runtime-interface-route-rule-triggering | evidence=2026-05-28 clean FAT live proof showed the previous `systemd.path` fix is not sufficient. On fresh `nixos-router-core-nebula`, `s88-provider-route-overlay-west-*` and `s88-provider-policy-rule-overlay-west-*` path units were active/waiting on `/sys/class/net/overlay-west`, but when `systemctl start s-router-nebula-render-node.service` later created `overlay-west`, the route/rule services stayed `inactive (dead)`. Starting those generated services manually installed the expected RPDB and policy-table defaults, changing `ip route get 1.1.1.1 from 10.20.70.100 iif upstream` from the underlay `client` path to `dev overlay-west table 2000`. The renderer now also emits a per-provider-interface `s88-provider-interface-<iface>` simple watcher that waits for the explicit provider interface and starts the generated route/rule units, while preserving the provider-owned interface boundary and path units. Focused proof passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-static-policy-rules.sh`. | expected=Provider-created overlay interfaces remain provider-owned and asynchronous. Renderer-owned route/rule materialization for those interfaces must be triggered by a robust runtime watcher when the provider interface exists, not rely only on `systemd.path` over `/sys/class/net`. | owner=network-renderer-nixos | reason=The NixOS renderer owns Linux route/rule materialization from explicit CPM/provider-neutral contracts. It must respect the provider lifecycle without inferring overlay topology or moving provider-specific Nebula runtime into the NixOS renderer.

- state=solved | target=dynamic-forwarding-nft-comment-handle-parser | evidence=2026-05-28 live `s88-dynamic-forward-*` journals printed `awk: $0 ~ "comment \\"" comment "\\"" { print $NF }` and `backslash not last character on line` while installing runtime source-file nft rules. The services exited success, but stale rule deletion was broken and repeated source-file changes could leave duplicate dynamic firewall rules. The generated script now uses `index($0, "comment \"" comment "\"")` instead of the invalid escaped regex, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dynamic-source-forwarding.sh` checks the generated parser text. `bash tests/test.sh` passed all renderer checks except this entry while it still said `fixed-locally`. | expected=Dynamic source-file nft services must be idempotent: find existing rules by exact generated comment, delete old handles, then install the current source prefix. | owner=network-renderer-nixos | reason=This is renderer-owned Linux nft materialization of explicit CPM source-file firewall contracts.

- state=solved | target=dns-service-forward-egress-exception-order | evidence=2026-05-28 live SAT triage proved Hetz DMZ Unbound recursion reached `hetz-router-core` as source `10.90.10.1`, then was dropped by public DNS leak-prevention before generic `upstream -> eth0` egress. CPM now emits source-scoped `dns-service-public-egress` forwarding intent. The renderer now includes `explicit-core-forwarding` rules in normalized explicit pairs, preserves source-prefix plus `trafficType = "dns"` matches together, and has `nft-allow-dns-service` insert `allow-dns-service-forward-egress` rules before public resolver forward drops. Proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-forward-egress-exception-order.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-cross-site-dns-service-render.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-direct-egress-block-tenant-scope.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh`. | expected=The renderer must materialize explicit CPM resolver-service egress exceptions ahead of leak drops without broadening endpoint direct DNS. The exception comment must not reuse `allow-dns-service-egress`, because that output-rule guard belongs to local Unbound output rules. | owner=network-renderer-nixos | reason=Once CPM supplies the source-scoped core DNS rule, the renderer owns Linux nft order and match preservation. Dropping `trafficType` when source prefixes exist or inserting only public resolver drops would keep the live SAT blocker.

- state=solved | target=provider-overlay-static-source-policy-rules | evidence=2026-05-28 live `nixos-router-core-nebula` installed provider-overlay routes through `s88-provider-route-overlay-west-*`, but the provider-created overlay has no NixOS-owned `10-overlay-west.network`; static source-scoped policy rules from CPM therefore could not be installed by networkd. The hostile path only worked after reversible live `ip rule` edits for static prefixes (`10.20.70.0/24`, `fd42:dead:beef:70::/64`) plus the runtime GUA prefix source on ingress `upstream` to table `2000`. After CPM `1b2eeaa` added explicit source-scoped core overlay forwarding intent, the renderer now emits provider-owned policy rule services for static source prefixes while leaving runtime source-file rules in the existing dynamic path. Focused proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-static-policy-rules.sh`, `tests/test-policy-only-routes.sh`, `tests/test-provider-overlay-interface-not-materialized.sh`, `tests/test-policy-source-scoped-routing-rules.sh`, `tests/test-dynamic-source-forwarding.sh`, and `tests/test-nix-file-loc.sh`. | expected=When CPM emits policy-routing rules for a provider-created overlay interface, the renderer must materialize static `From=<prefix> iif <ingress> table <provider-table>` and main-suppress fallback rules using a provider route/rule service, while keeping the overlay interface provider-owned and not creating a NixOS networkd unit for it. Dynamic source-file rules may remain in the existing dynamic rule path if CPM supplies `sourceFiles`. | owner=network-renderer-nixos | reason=The renderer owns Linux RPDB materialization from explicit CPM rules. It must not infer source scopes, but it also must not drop explicit static policy rules merely because the output interface is provider-created.

- state=solved | target=dns-direct-egress-block-tenant-scope | evidence=2026-05-28 live `nixos-router-core-nebula` blocked hostile overlay DNS to `10.90.10.1` because the generated `nft-allow-dns-service` script inserted `iifname "upstream" udp/tcp dport 53 drop comment "deny-direct-dns-egress"` on a p2p leg. CPM had `services.dns.directEgressBlockedTenants = [ "client" ]`, so the broad direct-DNS drop should have applied only to matching tenant interfaces; specific public resolver leak drops on upstream still need to remain. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-direct-egress-block-tenant-scope.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-cross-site-dns-service-render.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-only-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-interface-not-materialized.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=2 bash tests/test-policy-forward-default-paths.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=2 bash tests/cases/network-labs-example-output-contracts.sh` pass. | reason=This is renderer materialization of an explicit CPM DNS scope. CPM supplied a tenant-scoped direct-egress block; the renderer widened it to p2p, shadowing modeled overlay DNS traffic.

- state=solved | target=network-labs-safe-defaults-explicit-provider-overlay-interface | evidence=2026-05-28 focused rerun after freeing `/tmp`: `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=2 bash tests/test-network-labs-router-safe-defaults.sh` failed only for `s-router-overlay-dns-lane-policy` and `s-router-public-overlay-service` because the guard rejected `nebula1` in overlay-core nft rules. Current CPM for those examples explicitly emits `runtimeTargets.*.effectiveRuntimeRealization.interfaces.overlay-east-west.renderedIfName = "nebula1"` and `overlays.east-west.runtimeNodes.*.service.interface = "nebula1"`. The test now allows provider-shaped interface names only when the exact interface is explicit in CPM for that host/container, while still rejecting inherited WAN names (`eth0`/`eth1`), NAT masquerade, and MSS clamping on overlay cores. | expected=Renderer safe-default tests must fail on inferred provider/WAN leakage, but must not fail explicit CPM/provider interface materialization that the renderer is required to consume. | owner=network-renderer-nixos | reason=The NixOS renderer README allows materializing explicit provider-neutral overlay interface semantics from CPM/provider output. A test that blindly rejects `nebula1` contradicts the CPM contract and blocks the locked network-* sweep without identifying an implementation fault.

- state=solved | target=core-nebula-overlay-egress-nft-and-policy-route-leg | evidence=2026-05-28 focused artifact proof after `network-control-plane-model` 1b77c79 and the current NixOS renderer patch: CPM preserves the provider runtime interface name as `nebula1`; rendered `nixos-router-core-nebula` keeps only NixOS-owned networkd units `00-lo` and `10-upstream`, emits provider route services `s88-provider-route-nebula1-0` and `s88-provider-route-nebula1-1`, and renders nft accepts `iifname "upstream" oifname "nebula1" accept`, `iifname "nebula1" oifname "upstream" accept`, and `iifname "nebula1" accept comment "allow-overlay-to-core"` from explicit CPM/provider data. Focused tests passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-interface-not-materialized.sh`, `tests/test-policy-only-routes.sh`, `tests/test-cross-site-dns-service-render.sh`, and `tests/test-policy-cpm-firewall-parity.sh`. | expected=Provider-created overlay interfaces remain provider-owned, but explicit CPM/provider-neutral route and firewall contracts for those interfaces are still materialized as Linux policy-route services and nftables rules. The renderer must not create a `10-overlay-*` networkd unit for the tunnel and must not drop route/nft contracts merely because the provider owns the interface. | owner=network-renderer-nixos | reason=The NixOS renderer owns materializing explicit route and firewall contracts from CPM/provider-neutral interface facts. The broad normal-endpoint DNS/NAT66 live blocker is tracked in the lab/SAT runtime checks until a first wrong layer is proven; it is not a renderer-local solved regression.

- state=solved | target=provider-overlay-policy-table-route-materialization-and-dns-drop-order | evidence=2026-05-25 live hostile overlay proof showed CPM/debug contained policy-only overlay defaults, but the rebuilt runtime did not install the required Linux policy-table route because `overlay-west` is provider-created and intentionally has no `10-overlay-west.network`. The necessary live route was `nixos-router-core-nebula: ip route replace default dev overlay-west table 2000`, selected by `from 10.20.70.101 iif upstream`; this made hostile public egress traverse the overlay instead of the DHCP client underlay. The return path also needed `ip route replace 10.20.70.0/24 via 10.10.0.17 dev upstream onlink` so overlay replies returned to the local hostile fabric. Once public egress worked, direct `dig @1.1.1.1` from `nixos-hostile-node01` leaked over the overlay until explicit public-DNS drops were inserted before the broad `iif upstream oif overlay-west accept`; after that `ping 1.1.1.1` and `dig @10.90.10.1 example.com A` passed while `dig @1.1.1.1` timed out. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-only-routes.sh` now proves provider-created overlay policy-table route service inputs are retained while the overlay network unit remains provider-owned, and `tests/test-cross-site-dns-service-render.sh` proves public resolver forward drops use `nft insert` on core-nebula ingress before generic accepts. | expected=The renderer must materialize explicit CPM route/firewall contracts for provider-created overlay interfaces without creating a NixOS-owned overlay network unit. Policy-only defaults for provider overlay output must land in the selected policy tables, return routes must preserve modeled local prefixes, and direct public DNS leak-prevention drops must sort before generic overlay public-egress accepts. | owner=network-renderer-nixos | reason=The renderer owns Linux RPDB/route/nft materialization from explicit CPM/provider-neutral interface facts. It must not invent overlay topology, but it also must not drop explicit CPM policy-table routes merely because the tunnel interface is created by the provider renderer. ACCEPTED OVER-LIMIT: `s88/ControlModule/render/container-networks/interface-units.nix` remains the single networkd/interface-unit assembly point; moving provider-created-interface route extraction elsewhere before the route classes are normalized would duplicate policy-routing inputs and make route ownership less auditable.

- state=solved | target=policy-default-metric-tie-break-public-dns-egress | evidence=2026-05-25 `hetz-router-access-dmz` Unbound was active, listened on `127.0.0.1:53` and `10.90.10.1:53`, and emitted upstream DNS packets from modeled source `10.90.10.1`, but local `dig @127.0.0.1 google.com A` timed out. `hetz-router-policy ip route show table 2001` contained two equal metric 50 IPv4 defaults, `default via 10.80.0.15 dev up-dmz-ew` and `default via 10.80.0.17 dev up-dmz-wan`; `ip route get 1.1.1.1 from 10.90.10.1 iif downstream-dmz` selected the east-west leg. Reversible hotpatch `ip route replace default via 10.80.0.17 dev up-dmz-wan table 2001 onlink` made `hetz-router-access-dmz dig @127.0.0.1 google.com A` and `dig @10.90.10.1 google.com A` return `NOERROR` while direct public DNS leak blocks remained in place. The renderer now preserves explicit route metrics when projecting accepted forwarding defaults, so CPM-provided `up-dmz-wan` metric 1000 remains preferred over `up-dmz-ew` metric 2000 instead of both being forced to metric 50. Focused proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-downstream-default-route-lane-selection.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-forward-default-paths.sh`. | expected=The Linux policy table for `allow-hetz-dns-service-to-wan` must prefer the CPM-selected WAN output lane for public DNS forwarders, without removing explicit east-west routes for overlay/internal prefixes. | owner=network-renderer-nixos | reason=CPM emits selected forwarding pairs and route metrics; the renderer owns preserving those metrics during Linux policy-table projection. Equalizing explicit defaults with a renderer-local metric invented a kernel tie and sent public resolver egress to the east-west leg.

- state=solved | target=downstream-selector-access-table-return-route | evidence=2026-05-24 fresh full-loop build with `network-renderer-nixos 4311bad` still rendered `nixos-router-core-nebula` as a dynamic client with DHCP/SLAAC and correct DNS leak firewall, but `ping -c5 -W2 1.1.1.1` lost 5/5. Live `nixos-router-downstream ip route get 10.20.20.100 from 1.1.1.1 iif policy-client` selected `via 10.10.0.19 dev policy-client table 2001`; table 2001 had only policy-client defaults. A reversible patch adding `10.20.20.0/24 via 10.10.0.2 dev access-client table 2001 onlink` plus `fd42:dead:beef:20::/64 via fd42:dead:beef:1000::2 dev access-client table 2001 onlink` made `nixos-router-core-nebula ping -c5 -W2 1.1.1.1` receive 3/5 while `dig @1.1.1.1` still timed out. Dry rendering the real `s-router-overlay-dns-lane-policy` example showed the `policy-client` ingress rule targets table 2001, but the tenant return route via `access-client` was emitted in table 2007 instead of table 2001. The renderer now projects downstream selector policy-to-access return traffic into the selected access-facing table, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-router-diagnostic-main-routes.sh`, `tests/test-return-side-policy-rules-unscoped.sh`, and `tests/test-downstream-selector-return-connected-routes.sh` pass. | expected=The downstream selector access table selected by `iif policy-client` must contain the explicit tenant return route via `access-client` with `Table=2001`; the route must not exist only in the policy-client/fallback table 2007. | owner=network-renderer-nixos | reason=The renderer owns Linux RPDB/table materialization from explicit CPM forwarding pairs. WAN replies enter on the policy-facing interface, but the selected table is the accepted access-facing tenant table; emitting the tenant return route only in the policy-facing table leaves return traffic on the policy default and breaks underlay egress. ACCEPTED OVER-LIMIT: `s88/ControlModule/render/container-networks/policy-routing/raw-routes.nix` remains a single table-route projection unit; splitting during this access-table repair would obscure table construction order and duplicate selector-specific route selection rules.

- state=solved | target=downstream-selector-return-tenant-route | evidence=2026-05-24 live `nixos-router-core-nebula` had correct dynamic client underlay and reached the simulated ISP core, but `ping 1.1.1.1` failed because replies looped between `nixos-router-policy` `downstr-client` and `up-client-a`. `tcpdump` showed repeated `1.1.1.1 > 10.20.20.104` packets on `downstr-client` with decreasing TTL, and `nixos-router-downstream ip route get 10.20.20.104 from 1.1.1.1 iif policy-client` selected `via 10.10.0.19 dev policy-client table 2001`. A live route patch, `ip route replace 10.20.20.0/24 via 10.10.0.2 dev access-client table 2001 onlink` plus the IPv6 equivalent, made `nixos-router-core-nebula ping 1.1.1.1` pass while public DNS to `1.1.1.1` remained blocked. The renderer now emits downstream-selector tenant return routes via the accepted access interface for the policy table. Focused proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh`, `tests/test-policy-routing-source-interface-rules.sh`, `tests/test-downstream-selector-return-connected-routes.sh`, and `tests/test-overlay-underlay-access-network-render.sh`. | reason=The NixOS renderer owns Linux route-table materialization from explicit CPM forwarding pairs and tenant prefixes. A downstream selector policy table must route accepted return traffic for the local tenant prefix toward the access-facing interface; sending that destination back to the policy-facing interface creates an L3 loop and breaks underlay egress without changing firewall intent.

- state=solved | target=downstream-selector-return-tenant-route-policy-table | evidence=2026-05-24 fresh full-loop live build still missed `10.20.20.0/24` in `nixos-router-downstream` table `2001`; `ip route get 10.20.20.100 from 1.1.1.1 iif policy-client` selected `via 10.10.0.19 dev policy-client table 2001`. A reversible live patch adding only `10.20.20.0/24 via 10.10.0.2 dev access-client table 2001` plus `fd42:dead:beef:20::/64 via fd42:dead:beef:1000::2` made `nixos-router-core-nebula ping -c5 1.1.1.1` receive 4/5 while `dig @1.1.1.1` still timed out. The renderer now derives tenant keys for downstream access-facing interfaces, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-return-side-policy-rules-unscoped.sh` proves the policy-client table route is emitted on the access-client output network with the correct table ID and gateway. | reason=The prior regression checked the wrong generated network and did not derive tenant keys for `access-*` interfaces. Linux return traffic enters from `policy-client`, so the policy-client table must carry the access tenant return route. ACCEPTED OVER-LIMIT: `s88/ControlModule/render/container-networks/policy-routing/raw-routes.nix` is a table-route projection unit that centralizes mutually ordered route classes; splitting it during this live route repair would risk changing route ordering and hide the behavioral regression behind structure churn.

- state=solved | target=dynamic-tenant-client-networkd-materialization | evidence=2026-05-24 live `nixos-router-core-nebula` had the repaired underlay shape with only `client@if58`, but `/etc/systemd/network/10-client.network` disabled RA/link-local and did not enable DHCP. The core could not get an underlay address or route to `1.1.1.1` until that one unit was hotpatched to `DHCP=ipv4`, `IPv6AcceptRA=true`, and `LinkLocalAddressing=ipv6`; then it acquired `10.20.20.102/24` plus SLAAC addresses from the client tenant LAN. The renderer now consumes CPM `interfaces.*.dynamicAddressing` and emits the matching networkd DHCP/RA fields for logical tenant clients without static addresses. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-network-render.sh` covers the core-nebula tenant-client shape and verifies the renderer does not reintroduce p2p/overlay materialization or static default route guesses. | reason=CPM owns the explicit dynamic addressing contract; the NixOS renderer owns only Linux/systemd-networkd materialization of that contract. A logical tenant client with `dynamicAddressing` must use DHCP/SLAAC, while tenant interfaces without that field must keep the safe no-DHCP/no-RA default. The renderer must not infer this from `core-nebula`, `client`, missing addresses, or local lab names.

- state=solved | target=s-router-test-endpoint-egress-rpdb-return-and-lane-rules | evidence=2026-05-24 live `s-router-test` had cores online (`nixos-router-core-isp-a ping 1.1.1.1` passed) while endpoints had no internet. `tcpdump` proved `nixos-client01` ICMP left `nixos-router-access-client` and initially died at `nixos-router-downstream` because `ip route get 1.1.1.1 from 10.20.20.101 iif access-client` returned `Network is unreachable`; adding `from 10.20.20.0/24 iif access-client lookup 2001` moved requests forward. Replies then reached `nixos-router-policy` on `up-client-b` but `ip route get 10.20.20.101 from 1.1.1.1 iif up-client-b` selected a wrong uplink table; adding a return rule to the client table restored DNS. Replies finally reached `nixos-router-access-client` on `transit` but selected the transit table until adding `to 10.20.20.0/24 iif transit lookup 2001`; then `nixos-client01 ping -4 -c2 1.1.1.1` and `dig google.com A` passed. Focused renderer tests now pass: `tests/test-return-side-policy-rules-unscoped.sh`, `tests/test-policy-routing-source-interface-rules.sh`, and `tests/test-policy-source-scoped-routing-rules.sh`; full renderer sweep is the local gate and the full rebuild loop remains the downstream live gate. | reason=CPM already carries explicit forwarding pairs and tenant source prefixes; the NixOS renderer owns Linux RPDB materialization. Rule sources must come from explicit accepted forwarding pairs, not broad `policyRoutingSources` smearing, and scoped rules must also emit destination-scoped selectors so WAN replies can select the tenant return table instead of bouncing along the uplink/default table.

- state=solved | target=upstream-runtime-origin-source-lane-rules | evidence=2026-05-24 live `s-router-test` showed `nixos-router-core-nebula ip route get 1.1.1.1` selected `via 10.10.0.2 dev access-client src 10.19.0.8`, `nixos-router-policy ip route get 1.1.1.1 from 10.19.0.8 iif downstr-client` selected `via 10.10.0.39 dev up-client-b table 2001`, but `nixos-router-upstream ip route get 1.1.1.1 from 10.19.0.8 iif pol-client-b` selected `via 10.10.0.14 dev core-a table 2000` while nft only allowed `pol-client-b -> core-b`. Adding only `ip rule add pref 1999 from 10.19.0.8 iif pol-client-a table 2005` and `ip rule add pref 1999 from 10.19.0.8 iif pol-client-b table 2006` made `nixos-router-core-nebula ping -4 1.1.1.1` pass and traceroute show every internal hop through access-client, downstream, policy, upstream, and core-b. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-upstream-runtime-origin-source-lane-rules.sh`, `tests/test-runtime-origin-loopback-egress-render.sh`, `tests/test-downstream-selector-local-forward-policy-sources.sh`, `tests/test-policy-routing-source-interface-rules.sh`, `tests/test-policy-source-scoped-routing-rules.sh`, `tests/test-return-side-policy-rules-unscoped.sh`, and `tests/test-nix-file-loc.sh` pass after static and dynamic forwarding source scopes are applied per explicit `fromInterface -> toInterface` pair while downstream-selector policy interfaces keep their existing no-forwarding-source-scope guard. | reason=CPM already emits source-scoped runtime-origin forwarding pairs and lane-specific firewall accepts. The NixOS renderer owns Linux RPDB materialization and must not smear a target interface's source prefixes across every ingress source listed in broad policy-routing hints; source-scoped RPDB rules must preserve the exact CPM forwarding pair so a client-b packet cannot enter the core-a table, while downstream-selector policy interfaces must not duplicate those runtime-origin source rules into their own local tables.

- state=solved | target=return-policy-tables-preserve-explicit-local-destinations | evidence=2026-05-24 live `s-router-test` after `nixos-client01 ping 1.1.1.1` failed showed requests reached `nixos-router-core-isp-a` and replies returned to `nixos-router-upstream`, but `nixos-router-upstream ip route get 10.20.20.101 from 1.1.1.1 iif core-a` returned `Invalid cross-device link`. Adding only `ip rule add iif core-a lookup 2005 priority 1995`, `ip route replace 10.20.20.0/24 via 10.10.0.36 dev pol-client-a table 2005`, and the equivalent IPv6/client-b entries moved replies to `nixos-router-access-client`. They still died there because `nixos-router-access-client ip route get 10.20.20.101 from 1.1.1.1 iif transit` selected `via 10.10.0.5 dev transit table 2002`; adding only `ip route replace 10.20.20.0/24 dev tenant-client table 2002` and the IPv6 connected route made `nixos-client01 ping -4 1.1.1.1` and `ping -6 2606:4700:4700::1111` pass. | reason=The renderer owns Linux RPDB/table materialization from explicit CPM forwarding pairs and interface route facts. Return-side tables must not be broad main defaults, but they must include the explicit local/accepted destination routes needed to carry WAN replies back toward the tenant interface instead of bouncing replies back to the upstream/core interface.

- state=solved | target=downstream-runtime-origin-forward-rule-source-selection | evidence=2026-05-24 full-loop live `nixos-router-core-nebula ip route get 1.1.1.1` selected `via 10.10.0.2 dev access-client src 10.19.0.8`, `nixos-router-access-client ip route get 1.1.1.1 from 10.19.0.8 iif core-nebula` selected `via 10.10.0.5 dev transit table 2002`, but `nixos-router-downstream ip route get 1.1.1.1 from 10.19.0.8 iif access-client` returned `Network is unreachable`. `gron /etc/network-artifacts/control-plane.json | rg 'esp-nixos-router-downstream.*runtime-origin-egress|10\\.19\\.0\\.8|access-client|policy-client'` showed CPM explicitly emitted `forwardingIntent.rules[]` with `fromInterface = "access-client"`, `toInterface = "policy-client"`, and `sourcePrefixes = 10.19.0.8/32`; live RPDB had the client lane table but no `iif access-client lookup <policy-client-table>` selector capable of using that table. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-downstream-selector-local-forward-policy-sources.sh` now covers this direction. | reason=The renderer owns Linux RPDB materialization from explicit CPM forwarding rules. A policy table keyed by `toInterface` must include the rule's `fromInterface` as an ingress selector; route projection still uses accepted forward targets, but RPDB source selectors must also include explicit forward sources or modeled runtime-origin egress dies at the downstream selector.

- state=solved | target=upstream-selector-return-lane-rule-order | evidence=2026-05-24 full-loop live NixOS failed `nixos-client01 ping 1.1.1.1` after replies reached `nixos-router-upstream` on `core-b`. Live `nixos-router-upstream ip route get 10.20.20.100 from 1.1.1.1 iif core-b` failed/selected the wrong path because the rendered RPDB consulted `main suppress_prefixlength 0` for `iif core-b` before the lane table containing the policy-side return path. Adding only a table-first rule for core-side ingress into the client policy lane table made endpoint IPv4 pass. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-routing-source-interface-rules.sh`, `tests/test-return-side-policy-rules-unscoped.sh`, and `tests/test-policy-only-routes.sh` pass after routing rules are emitted table-first for every modeled source interface of the lane. | reason=The NixOS renderer owns systemd-networkd RPDB materialization of explicit CPM policy-only routes and lane/source metadata. Return traffic from the core side must select the same normalized lane table as policy-side egress; a main-table lookup must not win first, and this must not be repaired with a broad main/default route.

- state=solved | target=runtime-origin-source-reachability-origin-scope | evidence=2026-05-23 full-loop live tcpdump showed replies for `nixos-router-core-nebula` reached `nixos-router-policy` and were then forwarded to unrelated policy/upstream lanes because the rendered main table contained `10.19.0.8/32` host routes on many interfaces. CPM now emits origin metadata on runtime-origin `sourcePrefixes`; the renderer preserves that metadata from explicit forwarding rules and only emits Linux main-table source-reachability host routes on interfaces matching the modeled origin access when one is present. Focused renderer validation passes with `NETWORK_REPO_DIRECT_TEST_OK=1 NETWORK_CONTROL_PLANE_MODEL_OVERRIDE=path:/home/deadbeef/github/network-control-plane-model bash tests/test-runtime-origin-loopback-egress-render.sh`; full-loop live validation remains the downstream proof gate in network-codex-agent.  | reason=The NixOS renderer owns Linux source-reachability route materialization, but the selected origin lane is CPM data. The renderer may consume explicit `sourcePrefixes[].origin.accesses`; it must not infer the lane from names or local lab topology.

- state=solved | target=runtime-origin-main-source-reachability-route | evidence=2026-05-23 live `s-router-test`: `nixos-router-core-nebula` selected `1.1.1.1 via 10.10.0.2 dev access-client src 10.19.0.8`, `nixos-router-downstream` selected `via 10.10.0.23 dev policy-client table 2001`, but `nixos-router-policy ip route get 1.1.1.1 from 10.19.0.8 iif downstr-client` returned `RTNETLINK answers: Invalid cross-device link`. Adding only `ip route replace 10.19.0.8/32 via 10.10.0.22 dev downstr-client onlink proto static` on `nixos-router-policy` made the route lookup and `nixos-router-core-nebula ping -4` pass. The renderer now emits host-prefix source-reachability routes from explicit CPM `sourcePrefixes`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress-render.sh`, `tests/test-policy-source-scoped-routing-rules.sh`, `tests/test-router-diagnostic-main-routes.sh`, and `tests/test-policy-only-routes.sh` pass. | reason=The NixOS renderer already materializes CPM source-scoped policy rules and nft accepts for runtime-origin loopback egress, but Linux still requires a non-default main-table route back to the host source on the ingress interface before that forwarded source is accepted. This must be rendered from explicit CPM `sourcePrefixes`, not patched in `s-router-test`.

- state=solved | target=parallel-full-test-entrypoint-no-aggregate-timeout | evidence=2026-05-23 full-loop network repo sweep timed out `tests/test-passing-fixtures.sh` after 1800s while `tests/test.sh` was already running the same top-level checks as separate parallel jobs. The runner now excludes the legacy aggregate from auto-discovery, clears positional arguments before sourcing each test, and schedules `tests/cases/*.sh` directly alongside top-level `tests/test-*.sh`; README now names `bash tests/test.sh` as the conformance entrypoint. | reason=The renderer test entrypoint should maximize parallel validation and avoid one serial aggregate masking useful failures or timing out after the rest of the suite has produced actionable results. This is test harness behavior, not renderer policy materialization.

- state=solved | target=provider-overlay-removed-but-underlay-bootstrap-input-allowed | evidence=2026-05-23 parallel renderer sweep exposed `tests/test-public-overlay-service-forwarding.sh`: after provider-owned overlay interfaces were correctly removed, `b-router-core-nebula` no longer rendered the explicit UDP/4242 underlay input allow because core firewall logic required a NixOS-owned overlay ingress interface. The core firewall now derives underlay input ports from explicit CPM traffic types and allows them on modeled non-WAN core interfaces when no provider overlay interface is NixOS-owned. | reason=The NixOS renderer must not create provider overlay interfaces, but it still owns nftables materialization for explicit CPM underlay bootstrap traffic on the ordinary modeled underlay interface. Blocking the underlay port makes standalone `nix run network-renderer-nebula` impossible even though the topology and traffic type are modeled upstream.

- state=solved | target=overlay-route-retention-test-provider-boundary | evidence=2026-05-23 parallel renderer sweep exposed `tests/test-overlay-route-retention.sh`: the test still expected provider-owned `overlay-east-west` interfaces under `.render.nodes[*].interfaces`, which contradicts the current provider boundary. The test now asserts the overlay route facts remain present in `.debug.controlPlane.control_plane_model` and separately asserts NixOS render output does not materialize those provider-owned overlay interfaces. | reason=Renderer tests may inspect CPM/debug payloads for upstream route retention, but NixOS render output must not recreate Nebula tunnel interfaces or shared overlay host bridges that belong to the provider renderer.

- state=solved | target=segmented-policy-routing-without-main-default | evidence=2026-05-23 focused renderer checks prove NixOS keeps policy-only defaults inside CPM-selected RPDB tables instead of making segmented routers flat main-table routers: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-only-routes.sh` validates policyOnly IPv4/IPv6 defaults and return tables, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-upstream-selector-core-main-routes.sh` validates that only explicit core diagnostic defaults are present while selector roles keep tenant/default forwarding in policy tables. Primary references kept for the contract: Linux `ip-rule(8)` documents source/incoming-interface/table selection and the normal `main` fallback (`https://man7.org/linux/man-pages/man8/ip-rule.8.html`); `systemd.network` documents `[RoutingPolicyRule]` and table-scoped `[Route]` materialization (`https://www.freedesktop.org/software/systemd/man/254/systemd.network.html`); nftables hook/mark docs show firewall marking/drop is separate from RPDB route selection (`https://wiki.nftables.org/wiki-nftables/index.php/Configuring_chains`, `https://wiki.nftables.org/wiki-nftables/index.php/Setting_packet_metainformation`). | reason=The NixOS renderer owns systemd-networkd/RPDB materialization of explicit CPM routes and rules. It must not make segmented routers behave like flat home NAT routers by adding broad main-table defaults; endpoint validation must prove source/ingress route selection and live packets, not router-self ping alone.

- state=solved | target=standalone-provider-runtime-container-plumbing | evidence=2026-05-23 focused renderer checks prove the generic container plumbing required by standalone provider renderers is emitted by NixOS without embedding Nebula semantics: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-container-nix-flake-runtime.sh` proves rendered router containers include `experimental-features = nix-command flakes`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-host-build-container-selection.sh` proves container defaults can carry `CAP_NET_ADMIN`, `CAP_NET_RAW`, `CAP_SYS_ADMIN`, and `/dev/net/tun` exactly once. The s-router-test lab regression remains the live gate for reboot/runtime proof of the standalone `nix run github:esp0xdeadbeef/network-renderer-nebula -- render-node ...` command against real artifacts and profile files. | reason=The NixOS renderer/host container layer provides generic provider-runtime plumbing only: artifact/profile mounts, runtime secrets, package availability, Nix CLI feature enablement, and TUN/device capability. Nebula config, lighthouses, unsafe routes, and process semantics remain in the provider renderer/CPM output.

- state=solved | target=runtime-origin-loopback-egress-materialization | evidence=2026-05-22 live `s-router-test` after provider overlay removal showed `nixos-router-core-nebula` had no `overlay-west` interface, but `ip route get 1.1.1.1` still selected the p2p source and failed with `From 10.10.0.17 Destination Net Unreachable`; `nixos-router-upstream ip route get 1.1.1.1 from 10.19.0.8 iif core-nebula` returned `Network is unreachable`. After the first CPM/renderer patch, live `nixos-router-core-nebula ip route get 1.1.1.1` selected `src 10.19.0.8`, but upstream table `2002` still had default routes via `core-nebula` and `pol-hostile-ew` instead of the accepted WAN core. CPM now emits explicit `runtimeOriginEgress`, loopback host `sourcePrefixes`, and p2p default routes with `preferredSource`; `bash tests/test-runtime-origin-preferred-source-routes.sh` and `bash tests/test-runtime-origin-loopback-egress-render.sh` prove the renderer preserves `PreferredSource`, adds the accepted target default into the source-scoped upstream policy table, and does not add broad `iif core-nebula` table rules. | reason=The model owns whether runtime-origin loopback identity may egress. The NixOS renderer owns Linux materialization of explicit CPM route fields and source-scoped policy rules. It must not recreate provider overlay interfaces or infer WAN permission from `core-nebula` names, but it must render the route/rule fields CPM explicitly provides.

- state=solved | target=runtime-origin-access-firewall-source-scope | evidence=2026-05-23 full-loop live proof after the renderer lock bump showed live `nixos-router-access-client` now renders `iifname "core-nebula" oifname "transit" ip saddr 10.19.0.8 accept comment "runtime-origin-egress"` and the matching IPv6 accept. Before this patch, CPM emitted `forwardingIntent.rules[]` on `nixos-router-access-client` for `core-nebula -> transit` with `sourcePrefixes = 10.19.0.8/32` and `fd42:dead:beef:1900::8/128`, while live nft only had fallback `tenant-client <-> transit` accepts and direct-DNS drops. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress-render.sh`, `bash tests/test-dynamic-source-forwarding.sh`, `bash tests/test-policy-source-scoped-routing-rules.sh`, and `bash tests/test-dns-service-access-policy-exceptions.sh` now pass after the renderer accepts `explicit-access-forwarding` CPM rules and preserves `sourcePrefixes` in generic nft pair emission. | reason=CPM owns the source-scoped access forwarding permission; the NixOS renderer must materialize that explicit rule into nftables for access routers instead of falling back to local/uplink pairs or dropping source scope.

- state=solved | target=runtime-origin-policy-rpdb-source-scope | evidence=2026-05-23 live `s-router-test` after the access nft source-scope fix still had `nixos-router-core-nebula ip route get 1.1.1.1` selecting `src 10.19.0.8`, but `nixos-router-policy ip route get 1.1.1.1 from 10.19.0.8 iif downstr-client` returned `Network is unreachable`. `gron` over `/etc/network-artifacts/control-plane.json` showed CPM emitted `forwardingIntent.rules[]` for `downstream-client -> up-client-a/b` with `sourcePrefixes = 10.19.0.8/32` and `fd42:dead:beef:1900:0:0:0:8/128`; live nft had the matching source-scoped accepts, but live RPDB only had `from 10.20.20.0/24 iif downstr-client` and missed the runtime-origin loopback source. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress-render.sh`, `tests/test-policy-source-scoped-routing-rules.sh`, `tests/test-dynamic-source-forwarding.sh`, `tests/test-policy-forward-default-paths.sh`, and `tests/test-return-side-policy-rules-unscoped.sh` pass after normalized explicit forwarding pairs preserve `sourcePrefixes`, `sourceFiles`, and `family` for policy-routing rule generation. | reason=The renderer already normalized CPM interface names for nftables; policy-routing must consume the same explicit source scope after normalization. Dropping `sourcePrefixes` during pair-to-rule conversion turns a valid CPM rule into a firewall-only allow with no Linux route selection.

- state=solved | target=overlay-underlay-access-p2p-materialization | evidence=2026-05-22 `tests/test-overlay-underlay-access-network-render.sh` feeds CPM-shaped NixOS interface data where a selected access node has the overlay-underlay p2p without defaults, while the overlay core has a default route via that selected access and an unrelated upstream p2p with no default. The renderer emits `10-underlay-core-nebula` and `10-underlay-access-client`, preserves only the explicit core default via access, emits no selected-access default back into the overlay core, and does not recreate provider overlay units such as `10-overlay-west`. | reason=The NixOS renderer owns Linux/systemd-networkd materialization of explicit CPM interfaces and routes. The compiler/NFM/CPM own selecting the access underlay and route semantics; this renderer must neither infer them from names nor drop them when they are present.

- state=solved | target=upstream-selector-explicit-forwarding-projection-boundary | evidence=2026-05-30 renderer README audit found `tests/test-upstream-selector-no-core-crossconnect.sh` and `s88/ControlModule/firewall/policy/upstream-selector.nix` were enforcing a control-plane/topology semantic in the NixOS renderer by filtering explicit CPM forwarding pairs when both sides looked core-facing. That violated the renderer contract: CPM owns whether an upstream selector may have a core-facing pair; this renderer only projects explicit CPM/provider output. The renderer-side semantic gate was removed, `upstream-selector.nix` no longer passes a core-facing `shouldRenderPair` filter, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-upstream-selector-explicit-forwarding-projection.sh` proves an explicit CPM source-scoped pair is emitted unchanged. The owning semantic guard remains in `network-control-plane-model/tests/test-upstream-selector-no-core-crossconnect.sh`. | reason=Renderer tests may verify deterministic projection of explicit CPM fields, but they must not reject or repair topology/policy meaning that belongs to CPM/NFM/compiler. Core-facing cross-connect validity is an upstream control-plane contract, not a renderer-local policy decision.

- state=solved | target=core-dns-runtime-recursion-source-role | evidence=2026-05-22 live `s-router-test` after decoupling `s-router-test` from the Nebula renderer had no `/etc/network-renderer/network-renderer-nebula.json` and no `/persist/nebula-runtime`, but `nixos-router-core-nebula` still needed to run `nix run github:esp0xdeadbeef/network-renderer-nebula` manually. Live route proof showed `ping -4 -I 10.19.0.8 1.1.1.1` failed until `nixos-router-upstream` received a source-scoped route/rule for `from 10.19.0.8/32 iif core-nebula table 2108 via 10.10.0.12 dev core-a`, while `from 10.10.0.16 iif core-nebula` stayed unreachable as desired. After adding `src 10.19.0.8` to `nixos-router-core-nebula` default route, local Unbound could resolve via `127.0.0.1`; direct public DNS remained blocked by `deny-public-dns-output-leak`, matching the leak-prevention requirement. Upstream Unbound docs separate `interface`/`access-control` listener policy from `outgoing-interface` recursion source, and views/tags are for client-specific answers, not WAN source selection. `bash tests/test-dns-local-records.sh` proves the renderer consumes explicit CPM `services.dns.roles.recursion.outgoingInterfaces` for Unbound `outgoing-interface` and nft DNS service egress source, with the legacy flat `outgoingInterfaces` only as fallback. | reason=The renderer now materializes explicit CPM DNS roles instead of guessing from node/provider names. CPM still owns listener scope, recursion source addresses, and allowed upstream classes; the renderer must not solve DNS leaks by opening generic TCP/UDP 53 on upstream/WAN or by reparsing intent/inventory.

- state=solved | target=nebula-provider-boundary-host-reachability-materialization | evidence=2026-05-22 live `s-router-test` after removing the direct `network-renderer-nebula` import still had `nixos-router-core-nebula` running `overlay-west@if69` with `100.96.10.1/32` and `fd42:dead:beef:ee::1/128`; that meant the NixOS renderer was still materializing provider-owned overlay interfaces from CPM `sourceKind = "overlay"` as local veth/networkd state. `tests/test-provider-overlay-interface-not-materialized.sh` proves `s-router-test` NixOS output drops provider-owned overlay interfaces, extra veths, and `10-overlay-*` network units unless CPM explicitly marks the interface as NixOS-owned with `materialization.nixos.ownsInterface = true` or owner `network-renderer-nixos`. | reason=The NixOS renderer may render host/router realization for explicit NixOS-owned interfaces, p2p lanes, firewall, DNS, and routes, but provider-owned tunnel interfaces belong to the provider renderer/standalone service. A generic `sourceKind = "overlay"` CPM interface is overlay provisioning data, not permission for NixOS to create `overlay-west`; otherwise the lab silently reintroduces hidden Nebula runtime materialization after the direct renderer dependency is removed.

- state=solved | target=downstream-selector-crossconnect-gate-must-follow-cpm | evidence=2026-05-22 full-loop network-repo sweep failed `tests/test-downstream-selector-no-lane-crossconnect.sh` after all live route/DNS/hostile validators passed. The failing lines included downstream selector `access-client -> access-stream` Cast and `access-stream -> access-mgmt` DNS accepts. Inspecting the same dry render showed each line is backed by CPM `runtimeTargets[*].forwardingIntent.rules[]` with matching `fromInterface`, `toInterface`, and `relationId`; the test was still treating all same-side downstream selector forwarding as renderer-invented. The same stale assumption also made `tests/test-policy-zone-firewall-scoping.sh` reject the CPM-backed site-C `access-client -> access-dmz` DNS relation. Focused guards now pass: `bash tests/test-downstream-selector-no-lane-crossconnect.sh` and `bash tests/test-policy-zone-firewall-scoping.sh`. | reason=The renderer README allows materializing explicit CPM forwarding intent. The gates must reject unmodeled downstream selector cross-connects, not block legitimate CPM-modeled local service paths such as same-selector DNS/Cast relations.

- state=solved | target=dynamic-source-policy-rules-from-forwarding-sourcefiles | evidence=2026-05-22 full-loop hostile IPv6 egress reached Hetz but died at `fd42:dead:cafe:1000::11 !N`; live `hetz-router-upstream ip -6 route get ... iif policy-dmz-wan` returned `Network is unreachable`, and adding only `ip -6 rule add from 2a01:4f9:c01f:2::/64 iif policy-dmz-wan lookup 2004 priority 2004` made `nixos-hostile-node01 ping -6 2606:4700:4700::1111` pass. CPM now emits routed-prefix `sourceFiles` on the `policy-dmz-wan -> core` forwarding leg, and `bash tests/test-dynamic-source-forwarding.sh` plus `bash tests/test-policy-source-scoped-routing-rules.sh` pass after the renderer turns explicit forwarding source files into dynamic source-scoped policy rules for the ingress interface. | reason=CPM owns the source-file forwarding contract; the NixOS renderer owns Linux policy rule materialization. The renderer must consume explicit `forwardingIntent.rules[].sourceFiles`, not rediscover routed-prefix ownership from names or local route tables.
- state=solved | target=static-source-policy-rules-from-forwarding-sourceprefixes | evidence=2026-05-22 hostile IPv4 failed at Hetz until a live source-scoped rule was added on `hetz-router-upstream`: `from 10.20.70.0/24 iif policy-dmz-wan lookup 2004`. That single rule made hostile IPv4 ping pass from the endpoint. CPM now emits `forwardingIntent.rules[].sourcePrefixes` for the static IPv4 routed source on the same public-egress forwarding leg, and `bash tests/test-dynamic-source-forwarding.sh` proves the renderer emits both `From=10.20.70.0/24` policy rules and `ip saddr 10.20.70.0/24` nft accepts while preserving dynamic IPv6 source-file rules. | reason=The renderer owns Linux materialization of explicit CPM source scope. Static IPv4 prefixes must be consumed from CPM forwarding intent exactly like dynamic source-file IPv6 prefixes; inferring them from interface names, route tables, or tenant names would violate the renderer README.
- state=solved | target=upstream-policy-default-lane-filter | evidence=2026-05-22 full-loop hostile IPv6 failed with `nixos-router-upstream` table 2007 containing both `default via fd42:dead:beef:1000::26 dev pol-hostile-ew` and the intended `default via fd42:dead:beef:1000::10 dev core-nebula`; `ip -6 route get ... from 2a01:4f9:c01f:2::100 iif pol-hostile-ew` selected the policy peer. CPM only emitted the hostile lane default toward core-nebula on `pol-hostile-ew`; the extra default came from renderer projection of a core interface's unscoped east-west default back into the hostile policy table. A live hotpatch replacing table 2007 default with the core-nebula next hop made hostile endpoint `ping -6 2606:4700:4700::1111` pass. `bash tests/test-policy-only-routes.sh`, `bash tests/test-dynamic-source-forwarding.sh`, and `bash tests/test-policy-source-scoped-routing-rules.sh` pass after the renderer rejects unscoped projected defaults for lane-specific ingress tables. | reason=The renderer may project explicit forwarding defaults into Linux policy tables, but it must preserve CPM lane identity and not import an unscoped default whose lane does not match the target ingress lane.

- state=solved | target=explicit-forward-default-output-interface | evidence=2026-05-22 full-loop hostile overlay path validation showed `hetz-router-upstream ip route get 1.1.1.1 iif core-nebula from 10.20.70.100` selected `via 10.80.0.4 dev core` even though CPM emitted an explicit `core-nebula -> pol-dmz-ew` forwarding allow. `gron` over the current CPM artifact confirmed the explicit forwarding pair existed; renderer `raw-routes.nix` was first projecting default routes from the ingress/table interface instead of the explicit forwarding output interface, then live Hetzner validation showed the explicit output-interface default existed but lost to the generic core default because it had worse route metric. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-lane-route-scoping.sh`, `tests/test-sitec-dns-policy-table-lane.sh`, `tests/test-explicit-forwarding-suppresses-relation-fallback.sh`, and `tests/test-policy-forward-default-paths.sh` pass after the policy table uses the output interface routes and explicit forwarding defaults get a stronger metric than generic defaults. | reason=CPM owns the explicit forwarding pair; the NixOS renderer owns translating that pair into Linux policy-table routes. The renderer must not turn an explicit `fromInterface -> toInterface` pair into the ingress interface's own default path or render it too weakly to beat a generic WAN default in the same policy table.
- state=solved | target=forwarding-rule-family-scoped-default-projection | evidence=2026-05-22 full-loop hostile overlay path validation still showed `hetz-router-upstream` table 2001 picking `dev core` for IPv4 after the explicit route metric fix. `gron /etc/network-artifacts/control-plane.json` on the Hetzner validator showed CPM rule 12 is `family=6 core-nebula -> core`, but renderer `hasAcceptForwardingRule` ignored `family` when deciding which default routes to project into a policy table. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-lane-route-scoping.sh`, `tests/test-sitec-dns-policy-table-lane.sh`, `tests/test-explicit-forwarding-suppresses-relation-fallback.sh`, and `tests/test-policy-forward-default-paths.sh` pass after an IPv6-only accept can no longer project an IPv4 default. | reason=CPM may emit family-scoped forwarding intent. The NixOS renderer owns preserving that family scope when materializing Linux policy-table routes; treating a family-specific pair as `any` invents a route that CPM did not allow.
- state=solved | target=downstream-selector-local-forward-policy-sources | evidence=2026-05-21 full-loop live Hetz client-to-DMZ DNS still timed out after CPM emitted and nftables rendered `access-client -> access-dmz`; live `hetz-router-downstream` policy table for `access-client` had the client/policy routes but not the explicit DMZ prefix route. `bash tests/test-downstream-selector-local-forward-policy-sources.sh`, `bash tests/test-downstream-selector-return-connected-routes.sh`, and `bash tests/test-policy-routing-explicit-forward-pairs.sh` pass after explicit accepted forwarding targets extend, rather than get replaced by, CPM `policyRoutingSources`. | reason=CPM supplies explicit forwarding rules and policy-routing source hints. The NixOS renderer owns materializing both into Linux policy tables; a populated source hint must not erase another explicit CPM allow target.

- state=solved | target=kea-run-script-hook-breaks-dhcp4 | evidence=2026-05-21 full rebuild loop reached live validation and failed `test-examples-three-site-allowed-flows`; live access routers had `kea-dhcp4-client`, `kea-dhcp4-mgmt`, and `kea-dhcp4-hostile` restarting with `RUN_SCRIPT_LOAD_ERROR invalid path specified: '/run/kea-unbound-sync/...'`, and endpoint containers had IPv6 RA but no IPv4 leases. `tests/test-kea-unbound-lease-sync.sh` now proves the rendered Kea config omits `hooks-libraries` so DHCP startup is not gated by best-effort Unbound lease sync. | reason=The DHCP/DNS service is explicit CPM input, but the failed Kea hook path is NixOS service materialization. The renderer must keep lease-to-Unbound publication non-fatal and must not emit a hook path that prevents DHCP from starting.

- state=solved | target=kea-unbound-dhcp-hostname-sync | evidence=2026-05-21 live `s-router-test-clients` endpoint lookups for peer DHCP names only resolved through local stub/`/etc/hosts` behavior, while direct queries to access-node Unbound listeners such as `dig @10.20.20.1 nixos-client02.lan A` and `dig @10.50.20.1 clab-client02.lan A` returned no records. Live access-node Kea lease files already contained hostnames, but Unbound had no dynamic local data. `bash tests/test-kea-unbound-lease-sync.sh` and `bash tests/test-dns-local-records.sh` pass after the renderer enables Unbound remote-control and adds a timer-driven best-effort lease sync that must not gate container boot. | reason=The CPM DNS/DHCP service contract is explicit, but NixOS materialization of Kea lease hostnames into the selected Unbound implementation is renderer-owned service realization. This is not `/etc/hosts` resolver behavior and must remain hard-fail in tests while runtime sync failures stay non-fatal for container startup.

- state=solved | target=router-diagnostic-main-routes | evidence=2026-05-22 live hostile traceroute to `1.1.1.1` hid hops 2-4 until temporary main-table routes were added on `nixos-router-downstream`, `nixos-router-policy`, and `nixos-router-upstream`; the same traceroute then showed `10.10.0.7`, `10.10.0.25`, and `10.10.0.39`. `gron` over current NFM and CPM output showed `10.20.70.0/24` return routes are already present, so this was renderer materialization. `bash tests/test-sigma-router-diagnostic-main-routes.sh`, `bash tests/test-router-diagnostic-main-routes.sh`, `bash tests/test-upstream-selector-core-main-routes.sh`, and `bash tests/test-policy-only-routes.sh` pass after the renderer reuses the existing safe non-default diagnostic route projection for rendered `Gateway` routes, while keeping policy-only defaults out of `main`. | reason=CPM already supplies explicit non-default route facts for the allowed lane. The NixOS renderer owns Linux materialization: router-originated diagnostics do not have an ingress interface for `iif` policy rules, so safe non-default return reachability must also exist in the main table. The renderer must not infer new destinations or leak policy defaults.

- state=solved | target=return-side-policy-rules-unscoped | evidence=2026-05-21 live `s-router-test` showed tenant-sourced WAN egress from `nixos-router-access-client` reached `nixos-router-core-isp-b` and replies reached `nixos-router-upstream`, but return traffic later failed because rendered policy rules on `nixos-router-policy` `up-client-b` and `nixos-router-downstream` `policy-client` were scoped with `From=10.20.20.0/24`; WAN replies from `1.1.1.1` skipped those rules. Live `ip rule add ... iif up-client-b lookup 2009` and `ip rule add ... iif policy-client lookup 2007` made `ping -I 10.20.20.1 1.1.1.1` pass. `bash tests/test-return-side-policy-rules-unscoped.sh`, `bash tests/test-policy-source-scoped-routing-rules.sh`, `bash tests/test-downstream-selector-return-connected-routes.sh`, `bash tests/test-policy-downstream-default-route-lane-selection.sh`, and `bash tests/test-nix-file-loc.sh` pass after sharing the return-side source-scope decision in `policy-routing/rule-source-scope.nix`. | reason=CPM already provides tenant prefix owners, lane metadata, and policy table route facts. The NixOS renderer owns translating that CPM contract into Linux policy rules: source-side tenant ingress rules stay `From=<tenant-prefix>`, but return-side policy interfaces must use unscoped `iif` rules so WAN replies can select the modeled return table.

- state=solved | target=container-extra-veth-wait-online-deadlock | evidence=2026-05-21 full-loop live Hetzner validation failed Site-C DNS; live `hetz-router-core` was stuck activating in `systemd-networkd-wait-online`, its extra `upstream` veth stayed `NO-CARRIER`, and the host-side `hetc-p2phet-9a3` remained unbridged because the generated `ExecStartPost` bridge command cannot run until `systemd-nspawn` sends ready. `bash tests/test-container-networkd-wait-online.sh` now covers containers with both a primary host bridge and extra veths. | reason=The NixOS renderer owns container module materialization. Extra veth host-side bridge attachment is emitted as post-start runtime wiring, so rendered containers with any extra veth must not block readiness on networkd wait-online before that wiring has executed.

- state=solved | target=container-bind-mount-empty-path | evidence=2026-05-21 live `s-router-test` booted `/nix/store/pd5rrbdnnj1m6yn7f8c4y9z6z1qsgb26-nixos-system-s-router-test-25.11.20260518.687f05a`; every router container failed before init with `systemd-nspawn: Failed to parse --bind(-ro)= argument :: Invalid argument`. `/etc/nixos-containers/nixos-router-access-client.conf` contained `EXTRA_NSPAWN_FLAGS=" --bind-ro=: ..."` while the source-prefix secret bind mounts were otherwise valid. `tests/test-container-bind-mount-emission.sh` now proves empty bind mount destinations and empty host paths are not emitted while valid source-file mounts are preserved. | reason=The NixOS renderer owns translating explicit CPM/runtime source-file requirements into NixOS container bind mounts. Emitting an empty bind mount is invalid materialization, not an NFM/CPM topology decision or an s-router-test host hack.

- state=solved | target=policy-routing-source-prefix-scope | evidence=2026-05-21 upstream checks with `gron` proved `network-forwarding-model` emits `tenantPrefixOwners` for `10.20.70.0/24`, the hostile ULA `/64`, and `source:/run/secrets/access-node-ipv6-prefix-esp-nixos-router-access-hostile`, and `network-control-plane-model` preserves those owners plus the hostile `policy-derived-default` east-west lane. `bash tests/test-policy-source-scoped-routing-rules.sh`, `bash tests/test-policy-downstream-default-route-lane-selection.sh`, `bash tests/test-policy-only-routes.sh`, and `bash tests/test-dynamic-source-forwarding.sh` pass after the renderer emits `From=<tenant-prefix>` policy rules and dynamic source-file policy rules instead of broad `from all iif <lane> lookup <table>` rules. | reason=NFM owns source-prefix ownership, CPM preserves it, and the NixOS renderer owns materializing that explicit CPM contract into Linux policy rules. Recovering the source scope from interface names, local route tables, or firewall text would violate the renderer README.

- state=solved | target=policy-firewall-explicit-intent-cardinality | evidence=2026-05-20 rebuilt live nft/model comparison showed `rendered-host.json` and live nft matched, but both exceeded CPM forwarding intent: Hetz `allow-hetz-dns-service-to-wan` had one CPM relation and two rendered/live policy pairs. `tests/test-policy-firewall-explicit-intent-cardinality.sh` now proves explicit CPM forwarding intent replaces relation-expanded policy firewall rendering instead of being appended to it. | reason=When CPM supplies explicit forwarding pairs for a policy router, the renderer must materialize those pairs exactly. Re-expanding communication-contract endpoints in the renderer invents extra lanes downstream of CPM.

- state=solved | target=policy-downstream-default-route-lane-selection | evidence=2026-05-20 full loop failed the Hetz DNS-over-overlay probe: `dig -b 10.20.70.1 @10.90.10.1 example.com A` returned SERVFAIL/timeout. Live `hetz-router-access-dmz` had explicit CPM-rendered Unbound forwarders and DNS egress nft output rules, but its packet reached `hetz-router-policy` on `downstream-dmz` and `ip route get 1.1.1.1 from 10.90.10.1 iif downstream-dmz` selected `up-client-wan` table 2001 even after nft cardinality was fixed. CPM forwarding intent explicitly allows `allow-hetz-dns-service-to-wan` from `downstream-dmz` to `up-dmz-wan`, so routing selected a different lane than the CPM policy. `tests/test-policy-downstream-default-route-lane-selection.sh` and the full renderer suite now preserve the CPM-selected lane. | reason=The renderer was materializing policy downstream default tables from all upstream default routes and a later default-route projection branch still bypassed the CPM accept check when multiple WAN source interfaces existed, and route generation still consulted relation-expanded fallback pairs even when explicit CPM forwarding rules were present. This is a CPM-to-renderer loss, not an intent or inventory fact.

- state=solved | target=dynamic-delegated-prefix-return-policy-tables | evidence=2026-05-20 `bash tests/test-dynamic-source-forwarding.sh` proves dynamic delegated source-file routes are emitted into policy tables for downstream return paths, not only into upstream-selector core tables. | reason=CPM supplies explicit runtime-routed prefix return routes with `sourceFile`; the NixOS renderer must materialize those dynamic routes in every CPM-selected policy table that needs them instead of narrowing the projection to upstream core interfaces.

- state=solved | target=downstream-selector-return-connected-routes | evidence=2026-05-20 live `s-router-test` showed `nixos-router-access-client` packets reached WAN and replies returned to `nixos-router-downstream` on `policy-client`, but downstream table 2007 routed `10.10.0.2` back to `policy-client` because the table lacked the connected `10.10.0.2/31 dev access-client` route; live `ip route replace 10.10.0.2/31 dev access-client table 2007 src 10.10.0.3` made `ping 1.1.1.1` pass, and `bash tests/test-downstream-selector-return-connected-routes.sh` now covers the renderer output. | reason=Downstream selector return policy tables must include the connected access p2p subnet for accepted return interfaces. CPM already supplies the interface/address facts; the renderer must materialize the link-scope policy route.

- state=solved | target=public-ingress-runtime-forward-wan-hairpin | evidence=2026-05-20 live `s-router-test` hostile IPv6 public-egress reached `nixos-router-core-nebula` and selected `nebula1` toward Hetzner, but local Nebula handshakes to `hetz-router-nebula-core` timed out. Live Hetz nftables had `iifname != "br-wan" ip daddr @s88_public_runtime_0 udp dport 4243 dnat ip to 172.31.254.4`, while the local Nebula static map explicitly targeted the Hetzner public IP on UDP/4243. `tests/test-public-ingress-module.sh` now asserts runtime public forwards accept WAN ingress on the host bridge; `bash tests/test-public-ingress-module.sh`, `bash tests/test-nix-file-loc.sh`, and `bash tests/test-passing-fixtures.sh` pass. | reason=Public runtime forwards are explicit CPM/runtime facts and must be reachable from the modeled public address and dport. The renderer should constrain them by public-address set and modeled port, not exclude the WAN bridge that carries the public packet.

- state=solved | target=public-ingress-runtime-forward-input-dports | evidence=2026-05-16 `runtimeFacts.publicIngress.runtimeForwards[*].inputDports` was being repeated in `s-router-hetzner-anywhere` to open the public-forward container input path for modeled ports 4242/4444/4445/4446. `s88/ControlModule/module/public-ingress/facts.nix` now derives missing runtime-forward input dports from explicit CPM external service relations and traffic-type matches, and `bash tests/test-public-ingress-module.sh` passes without setting `inputDports` in the runtime fact. | reason=The dports are explicit CPM public service contract data. The NixOS renderer should project those dports into container-side runtime-forward input rules instead of requiring a host-specific NixOS port-list monkeypatch.

- state=solved | target=sigma-diagnostic-main-route-projection-scope | evidence=2026-05-21 full-loop live validation failed `site-c-ipv4-a` DNS: `nixos-hostile-node01 dig @10.90.10.1` timed out and `nixos-router-downstream ip route get 10.90.10.1 from 10.20.70.100 iif access-hostile` selected main-table `via 10.10.0.19 dev policy-admin` before the source-scoped hostile table. Deleting that one main route moved the packet onto the hostile lane; it then died on Hetzner because `hetz-router-upstream ip route get 10.90.10.1 from 10.20.70.100 iif core-nebula` selected main-table `via 10.80.0.16 dev policy-dmz-wan` instead of overlay `pol-dmz-ew`. Deleting that second wrong main route made the hostile DNS probe return `NOERROR`. `bash tests/test-sigma-router-diagnostic-main-routes.sh` now covers the required hostile diagnostic return routes and forbids both wrong site-C main routes; `bash tests/test-public-overlay-service-forwarding.sh` keeps host-specific service-ingress DNAT routes working. | reason=Renderer materialization may add safe main-table diagnostic return routes for router-originated ICMP/traceroute, but only for explicit `internal-reachability` routes returning to the same modeled access lane. Service-ingress main-table projection is only for endpoint destinations, not tenant-wide prefixes that would override source-scoped policy routing.
