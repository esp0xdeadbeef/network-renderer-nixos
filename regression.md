# Regression Notes

Keep this file current with solved or intentionally accepted repository-local
test-gate findings. Unresolved implementation defects should name the failing
command, evidence, owner, and next fix.

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 deterministic local-only refusal | first-bad-artifact=the first exact cold stage returned `NXDOMAIN` instead of `REFUSED` because the renderer appended `local-zone: "." static`; the first construction fix then made every local-only `allowFrom` prefix `refuse_non_local`, and the second exact cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-refusal-20260718` proved that this also refused the permitted forwarded `shared.lab` namespace | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-540-HDS-010-SDS-010-SMS-035.sh` requires ordinary local-only clients to remain `allow`, relation-scoped lateral requesters to remain `refuse_non_local`, and the terminal catch-all to be `local-zone: "." refuse` rather than `static`, and passes. `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` passes 109/109 | reason=Unbound documents `refuse_non_local` as static-local-data-only, so it cannot express the namespace-forwarding contract; the root refuse zone materializes CPM's miss action while the more-specific transparent/forwarded namespaces remain usable; new pushed cold-stage evidence remains required

- state=solved | target=FS-270-HDS-010-SDS-010-SMS-020 relation-scoped policy-state route selection | first-bad-artifact=the pinned CPM emitted four portable selectors for forward/return and IPv4/IPv6 plus exact bounded routes in the selected policy tables, but the NixOS renderer ignored `effectiveRuntimeRealization.routeSelectionRules`; systemd-networkd therefore retained generic ingress rules that could choose a direct destination-access route before the modeled policy state owner | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-270-HDS-010-SDS-010-SMS-020.sh` was red with four CPM selectors/routes but zero rendered rules, then proved four exact family/source/destination/ingress/priority/table RoutingPolicyRules and four selected-table routes; seeded negatives reject wrong tables, absent ingress, absent return behavior, absent reachability, and default/transitive authority. `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=42 bash tests/test.sh` passes 109/109 against the pushed CPM and spec pins | reason=the renderer now projects only complete CPM route-selection authority and fails closed on incomplete or ambiguous bindings; it does not infer relation routes, tables, priorities, or egress meaning

- state=construction-fixed-live-pending | target=FS-540-HDS-010-SDS-010-SMS-045 dual-stack local DNS return path | first-bad-artifact=the 2026-07-18 pushed cold stage passed NixOS recursive/core and local-sharing UDP/TCP checks, then the IPv6 reverse-local query timed out; CPM carried both local-dns service addresses and both table-1002 return routes, but the NixOS renderer emitted only the IPv4 host-source rule because it compared compressed and expanded IPv6 text literally | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-045.sh` proves equivalent compressed and expanded IPv6 service-address forms produce the locally originated IPv6 rule without incoming-interface context; the complete `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=42 bash tests/test.sh` sweep passes 108/108, including the corrected non-special-use controlled-authority namespace | reason=the renderer now compares IPv6 address identity through the Nixpkgs IPv6 parser while preserving CPM's source address, route table, and priority in emitted rules; new pushed cold-stage NixOS and CLAB evidence remains required

- state=construction-fixed-live-pending | target=FS-540-HDS-010-SDS-010-SMS-045 valid controlled-provider RA source | first-bad-artifact=the 2026-07-18 exact-source cold stage observed the core RS on both ends of `isp-primary` and dnsmasq logged `RTR-ADVERT`, but the isolated provider bridge rendered `LinkLocalAddressing=no`; captures saw no RA packet because the authority had no valid link-local source | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-045.sh` is red unless the controlled provider opts its bridge into IPv6 link-local addressing while retaining forwarding and autonomous SLAAC | reason=the harness authority now overrides only its selected isolated bridge to `LinkLocalAddressing=ipv6`; the rejected core `accept_ra=2` lifecycle repair was removed because systemd-networkd intentionally disables kernel RA and consumes advertisements in userspace | live-boundary=a new pushed exact-source cold stage must observe a link-local-sourced RA, first-boot selected IPv6 address/default, and the full dual-stack SMS

- state=construction-fixed-live-pending | target=FS-540-HDS-010-SDS-010-SMS-045 functional IPv6 provider router | first-bad-artifact=the 2026-07-18 exact-source cold stage rendered autonomous SLAAC and logged RS/RA activity, but an exact capture saw the core RS leave `wan0` and no RA return while the controlled NixOS authority host had IPv6 forwarding disabled | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-045.sh` was red on the absent `net.ipv6.conf.all.forwarding` setting and now requires it together with autonomous SLAAC and the controlled authority | reason=the harness-scoped authority module now materializes a functional IPv6 router surface instead of treating dnsmasq configuration or logs as dataplane proof | live-boundary=a pushed exact-source cold stage must still prove first-boot selected IPv6 address/default acquisition and the full dual-stack SMS

- state=construction-fixed-live-pending | target=FS-540-HDS-010-SDS-010-SMS-045 autonomous provider SLAAC | first-bad-artifact=the 2026-07-18 exact-source cold stage carried `ipv6.method = "slaac"` through the pushed lab inventory and CPM, but the NixOS authority fixture rendered dnsmasq `ra-only`; the core therefore saw RA activity without receiving a global IPv6 address or selected-table default, and the row stopped at `selected-egress6-nixos` | evidence=the network-labs SMS now rejects RA visibility without the autonomous A-bit, and `bash tests/FS-540-HDS-010-SDS-010-SMS-045.sh` was red before repair and now requires `ra-only,slaac` in the rendered provider range | reason=the renderer now preserves the modeled autonomous-SLAAC contract while retaining RA-only DHCP suppression | live-boundary=a new pushed exact-source cold stage must pass the dual-stack NixOS/CLAB protocol before the SMS can become OK

- state=construction-fixed-live-pending | target=FS-540-HDS-010-SDS-010-SMS-045 controlled iterative authority | first-bad-artifact=the pushed lab source and CPM carried an isolated dual-stack root/delegation/terminal authority on the selected provider, but the NixOS renderer ignored it and an isolated uplink without `parent` crashed host rendering | evidence=`bash tests/FS-540-HDS-010-SDS-010-SMS-045.sh` proves CPM-only host materialization, controlled root hints, disabled public root trust, dual-stack DHCP/RA, root/delegation Knot authority, selected-provider addresses, an unanswered alternate provider, and seeded selected-egress divergence; the complete six-container NixOS system closure builds and validates both authority zone files | reason=the renderer consumes only `services.dns.validationAuthority`, materializes the harness provider on the explicit isolated bridge, and leaves ordinary services unchanged; host uplink grouping now treats `parent` as optional only for isolated bridges | live-boundary=cold NixOS/CLAB protocol evidence remains required

- state=solved | target=FS-525/FS-540 deterministic iterative DNS egress under multiple uplinks | first-bad-artifact=after the pushed CPM and renderer commits were cold-staged, `s-router-nixos` emitted the exact DNS-only mark plus dual-stack fwmark rules and put the DHCPv4 default in CPM table 1002, but the selected SLAAC/RA IPv6 default remained only in `main`; marked IPv6 DNS therefore fell through the empty table and could still follow candidate order | evidence=`FS-540-HDS-010-SDS-010-SMS-035.sh` now requires the selected SLAAC interface to set `IPv6AcceptRA.RouteTable` to the exact CPM table alongside the dual-stack fwmark rules; `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=42 bash run-all-tests.sh` passes 92/92 | reason=systemd-networkd now maintains DHCPv4 and RA IPv6 defaults in the same model-owned table across renewals without renderer-allocated IDs. A new pushed cold-stage remains required for downstream SIT acceptance.
- state=construction-fixed-live-pending | target=FS-525/FS-540 reproducible DNS authority materialization | first-bad-artifact=the renderer accepted named-core resolver records without terminal `endpointAuthority`, ignored provider `serviceEndpointBindings`, and allowed a `static` local zone to shadow the same modeled forward namespace | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-035.sh` compiles the pushed isolated lab through CPM and proves access forwarders and core listeners share the exact relation/terminal authority, bilateral source-scoped `refuse_non_local`, forwarding-compatible local zones, iterative core recursion, privacy-safe warning surfacing, and fail-closed seeded authority, listener, namespace-shadow, recursion-leak, divergence, and fatal-warning cases | reason=the renderer consumes and validates CPM authority without endpoint or policy rediscovery; it emits `DNS_RENDERER_CONTRACT_DIVERGENCE` or `DNS_LOCAL_NAMESPACE_SHADOWED` without address material when the emitted service would not be reproducible. Live NixOS and CLAB parity remains required before the row can close.
- state=fixed | target=FS-180 explicit return behavior in runnable compatibility fixtures | evidence=`TEST_JOBS=42 NETWORK_REPO_DIRECT_TEST_OK=1 tests/test.sh` passes 105/105, including all 15 runnable pinned network-labs sources and `tests/test-policy-zone-firewall-scoping.sh` | reason=the current CPM correctly rejects implicit allow direction; legacy renderer fixtures now declare their historical forward-only behavior as `one-way`, and the scoping assertion proves the client-to-DNS path traverses policy rather than accepting a direct access-to-access bypass.
- state=fixed | target=FS-860 DHCPv4 persistent lease-state write boundary | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-dhcp4-pipeline-render.sh` preserves explicit `/var/lib/kea/client.leases` from the pinned CPM advertisement through its state contract and asserts `StateDirectory=kea`; `tests/test-kea-unbound-lease-sync.sh`, `tests/test-kea-dhcpv6-render.sh`, and `tests/test-lease-state-mode-validation.sh` pass on canonical main | reason=the renderer now supplies service write access for the explicit CPM storage binding, matching DHCPv6 without requiring a host-profile service patch or renderer-local lease-path invention.
- `s88/ControlModule/render/container-networks/interface-units.nix`: ACCEPTED OVER-LIMIT. Single responsibility: emits container interface systemd network units from explicit runtime interface records. Splitting would scatter shared ordering and naming invariants across smaller files.
- `s88/ControlModule/render/container-networks/policy-routing/raw-routes.nix`: ACCEPTED OVER-LIMIT. Single responsibility: normalizes raw policy-route records for renderer emission. Splitting would separate common validation from route-shape handling.
- state=fixed-locally | target=s88/ControlModule/render/systemd-host-network/local-bridges.nix host bridge addresses | evidence=NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-fs310-hds020-sds010-sms200-nixos-bridge-no-default.sh | reason=NixOS renderer must not assign hardcoded 10.11.0.1/24 to CPM/runtime bridges absent explicit bridgeNetworks.hostAddresses, DHCP, or SLAAC authority.
- state=fixed | target=kea-dhcp6 runtime server DUID state | evidence=renderer `7709e44` adds the writable Kea StateDirectory; focused DHCPv6 render test passed and the 2026-07-17 post-rebuild FS-970 live verifier found kea-dhcp6-client active with the exact protected lease | reason=the standalone DHCPv6 unit owns writable server-identifier state instead of failing configuration load under a read-only service filesystem.
- state=fixed | target=Kea access-interface and listener readiness | evidence=renderer `2606821` adds operational-interface preflight and UDP-listener postflight; focused DHCPv4/DHCPv6 tests passed and the 2026-07-17 post-rebuild FS-970 live run completed dual-stack lease exchange | reason=service startup now rejects an interface without operational carrier and rejects a process that did not open its modeled listener.
- state=fixed | target=modeled DHCP service input firewall | evidence=renderer `4acb741` emits family-, interface-, and port-scoped DHCP ingress; focused firewall/render tests passed and the 2026-07-17 post-rebuild FS-970 client completed DHCPv4 and DHCPv6 exchanges through the drop-default client-edge policy | reason=each explicit DHCP advertisement now derives only its own local input allowance.
- state=fixed | target=protected reservation allocation mode | evidence=renderer `01a0190` validates protected addresses outside the modeled pool and enables Kea out-of-pool reservation allocation; seeded materializer negatives passed and the 2026-07-17 post-rebuild FS-970 client received both protected addresses | reason=runtime materialization now fails closed for ambiguous in-pool records and explicitly selects the supported Kea allocation mode.
- state=fixed | target=Kea DHCPv6 DUID representation | evidence=renderer `ea7e18b` emits canonical colon-delimited DUID byte pairs; focused materializer tests passed and the 2026-07-17 post-rebuild FS-970 run matched the SOPS, runtime, packet, and client DUID bytes before granting the protected IPv6 lease | reason=accepted compact or delimited protected input is normalized to Kea's canonical reservation form.
- state=fixed | target=Kea DHCPv6 subnet-to-interface binding | evidence=renderer `7e7758e` binds subnet6 to the modeled interface; the focused test failed before the fix and passed after it, then the 2026-07-17 post-rebuild FS-970 live verifier proved exact predictable IPv4 and IPv6 leases with no extra global client addresses | reason=the link-local-only Kea access interface can now select its authoritative subnet before protected reservation lookup.
- state=fixed | target=protected reservation mixed pool allocation | evidence=renderer `1ea9d7b` preserves valid in-pool and mixed protected recordsets, explicitly enables in-subnet lookup, and derives out-of-pool lookup from the protected set; focused materializer positives and negatives passed, then the 2026-07-17 non-activating s-router-prod check decrypted the migrated SOPS sources in a temporary mode-0700 boundary, materialized all 48 VLAN2 and 1 VLAN3 records with exact source parity and mode 0600, and the production Kea 3.0.3 parser accepted both configs | reason=the runtime materializer now supports the allocation modes already supported by Kea without requiring protected production addresses or modeled pools to be changed or disclosed.
- state=fixed | target=runtime delegated-prefix tenant route derivation | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-350-HDS-010-SDS-010-SMS-060.sh`; NFM `af528b2` and CPM `afadaa3` emitted 22 production route instances with complete derivation metadata; the non-activating `s-router-prod` candidate built successfully with 39 native route units; realized VLAN2/3/7 units used slots 2/3/7 for `/48` to `/64`; protected live SOPS sources passed mode, canonical-parent, distinct-child, containment, and redaction checks without printing values | reason=the renderer derives the exact per-tenant prefix from the protected parent at runtime, preserves derivation identity during deduplication, rejects malformed/mismatched sources without disclosure, and never installs the raw delegated parent as a tenant route.
- state=fixed | target=CPM-owned public-ingress NAT and post-DNAT forwarding | evidence=`bash tests/test-fs310-hds020-sds010-sms075-core-ingress-dnat-materialization.sh`; the production `s-router-prod` CPM artifact rendered exact TCP/UDP 4242 DNAT, `ct status dnat` forward, source rewrite, and target-/32 forwarding on core, upstream-selector, policy, downstream-selector, and access; all seven container rulesets passed `nft --check` in isolated network namespaces | reason=the renderer consumes `natIntent.publicIngress` and exact CPM forwarding matches without requiring a public-address literal, preserves stateful-return, and does not infer policy from inventory or host-local runtime forwards.
- state=fixed | target=identical logical uplink aliases on one physical parent | evidence=`bash tests/cases/external-examples.sh` against network-labs `d45a8611`; the 104-test renderer sweep passed including the HAT `wan`/`inter-site` alias realization | reason=multiple logical uplink names may share one parent only when their bridge and host-network realization are identical; genuinely different direct bridges on one parent still fail closed.
- state=construction-fixed-live-pending | target=FS-540 direction-scoped recursive DNS egress | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage12` used pushed NixOS renderer `6a894b55e4f6810a13920bc063a8ed72739e760c`; core received and completed recursive requests, but every internal answer timed out because the renderer-added `User = "unbound"` rule selected provider table 1002 for the reply as well as the upstream request | evidence=with the same core source and internal destination, `ip route get` used the modeled transit return without a UID and the WAN table with the real Unbound UID; CPM supplies the selected interface, table, priority, and firewall mark but no process-wide UID selector, so this is a representative FS-540 renderer invention; the focused FS-540 test was made red before repair and now requires exactly two dual-stack fwmark rules with no resolver-identity rule | reason=the renderer now selects only upstream DNS requests through CPM's destination-port mark; internal replies from the same process retain the normal modeled route. A fresh pushed cold stage remains required for live acceptance.
- state=construction-fixed-live-pending | target=FS-540 pre-socket recursive DNS route selection | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage13` used pushed renderer `36b219bdc838a49f36ed5e5a5d26e4406090d28a`; the valid no-UID runtime preserved internal reply routes, but the first NixOS recursive query returned SERVFAIL and captures on `wan0` and `any` saw zero DNS packets | evidence=the selected fwmark table resolved both families when the mark was supplied explicitly, while a resolver-independent UDP/53 socket probe failed with `ENETUNREACH` before packet emission; the nft output hook therefore cannot create the initial socket route. The revised pushed SDS/SMS requires resolver identity plus UDP/TCP destination port 53 at pre-socket policy lookup and rejects both output-mark-only and process-wide UID selection. The focused FS-540 construction test was red before repair and now requires exactly four scoped resolver rules plus the two dual-stack fwmark rules | reason=the renderer now materializes protocol- and destination-port-scoped resolver identity rules in addition to CPM's packet mark, so upstream sockets obtain the selected provider route while replies to requester ephemeral ports retain the modeled return path. Fresh pushed cold-stage evidence remains required for downstream acceptance.
