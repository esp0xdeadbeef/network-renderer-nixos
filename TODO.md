Make the correct filestructure based on the system prompt.

Hard fail on any assumption, don't write assumptions, there are only options, 
Example:
If DHCP is enabled - ranges MUST be defined to let kea work


i want a folder structure like this: 
./work/etc/network-artifacts/<enterprise>/<site>/<host>/<hostdata>and<container>/<<specific-service-containerdata>.json>
